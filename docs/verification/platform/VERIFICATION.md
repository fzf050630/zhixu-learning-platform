# 平台验收记录

## 第二版线上部署（2026-09-19）

- 目标：部署到已有站点根目录下的独立子目录（与同域其它站点互不影响），全站使用相对路径，无需改动 Nginx 配置。
- 流程：本地 `npm run build` 打包 `dist/` → 上传 → 先备份上一版 → 目录替换 → 权限归一（目录 755 / 文件 644）。
- 校验：本地与线上 249 个文件 SHA-256 全一致；线上入口 HTTP 200，`platform/tokens.css` 含新版 MiSans 字体栈，目录条目 240 条，四科算法板块与数据结构 54 个实验说明均可访问。
- 回退：恢复部署前的备份包即可回到第一版。

---

## 第四轮：字体重构（2026-09-19）

- 字体栈切换：全平台（门户、七科页面、共享页头与画布文字）首选 **MiSans**，回退顺序为 MiSans Demibold → HarmonyOS Sans SC → 思源黑体 CN → Source Han Sans SC → PingFang SC → 微软雅黑；六科画布 `FONT_SANS` 与数据结构画布中文回退同步更新。
- 门户加粗与强调：主标题 800 字重 / 48px 上限、区块标题 800、卡片标题 800、侧边导航 600、元信息 600、按钮与入口 700、英文标签与角标 700 并加大字距。
- 验证：`npm test` 2/2、根浏览器 10/10、数据结构 64/64 + 24/24、audit 七科 0 问题、`verify-all` 240/240 无失败与文本异常、画布文字溢出扫描与切换前一致（仅余旋转轴标签等已知检测器假象）、`verify-ui` 0 错误。

---

## 第三轮：算法流程板块 + 全平台字体优化（2026-09-19）

### 交付

- **算法流程板块**：408 四科中所有出现算法的小节新增统一板块「算法流程」，包含算法名称与类别、复杂度、做什么、输入与前提、执行步骤（01 起编号）、关键点与易错；数据结构 54 个实验全部新增 `explain` 说明字段。合计 84 个板块 + 54 份实验说明：
  - 计算机组成原理 24 个（进制转换、补码与溢出、IEEE 754、浮点加减、原码/Booth 乘法、加减交替除法、存储扩展、Cache 访问与三种映射、FIFO/LRU/CLOCK、页表地址转换、有效地址、扩展操作码、指令周期、微程序、五段流水线、冒险处理、程序查询、中断、DMA 等）。
  - 操作系统 31 个（系统调用、进程切换、FCFS/SJF/SRTN/HRRN/RR/优先级/多级反馈队列、PV 操作、生产者-消费者、读者-写者、哲学家、银行家算法、死锁检测、动态分区分配、分页地址变换、OPT/FIFO/LRU/CLOCK、文件分配、目录检索、位示图、成组链接、I/O 请求流程、缓冲、SPOOLing、FCFS/SSTF/SCAN/C-SCAN）。
  - 计算机网络 29 个（时延计算、奈氏与香农、曼彻斯特与差分曼彻斯特、字节填充、零比特填充、CRC、海明码、停止等待、GBN、SR、CSMA/CD、二进制指数退避、交换机自学习、子网划分、CIDR 聚合、ARP、DHCP、距离向量、链路状态、最长前缀匹配、三次握手、四次挥手、Karn 算法、快速重传、慢开始与拥塞避免、快恢复、DNS、电子邮件）。
- **页内字体优化**：六科正文 15.5px → 16.5px、小节标题 30px → 33px、卡片标题与表头加粗，目录、表格、公式、读数、图例、徽标等全部上调 1～1.5px；数据结构正文 15px → 16px、伪代码 14px → 15px；门户 14px → 15px；共享页头同步上调。长伪代码保持自动换行。

### 执行结果

| 检查 | 结果 |
| --- | --- |
| `npm test` | 2/2 通过 |
| `npm run audit` | 七科 0 问题 |
| `npm run test:browser`（根） | 10/10 通过 |
| 数据结构 `npm test` | 64/64 通过 |
| 数据结构 `npm run test:browser` | 24/24 通过 |
| `node scripts/check-source.cjs`（七科源码直检） | 计组 39/39、操作系统 16/16、网络 21/21、数据结构 54/54，均 0 问题页面 |
| `node scripts/verify-all.cjs`（发布产物 240 入口） | 0 失败页面、0 文本越界或重叠（字号调大后复检通过） |
| 算法板块结构自检 | 84 个板块 goal/inputs/steps/keys/cost 齐全，steps 5～8 步、每步至多 1 处加粗 |
| 数据结构 explain 自检 | 54/54 实验均有 goal/inputs/steps/keys/cost |

### 回退

新增内容集中在各科 `content/ch*.js` 与数据结构 `content/chapters/*.js`、`content/extensions.js`、`content/advanced-tree-experiments.js`；渲染支持位于三科 `assets/js/app.js`（`case 'algo'`）与数据结构 `assets/js/app.js` + `index.html`（`#algoPanel`）；样式位于六科 `assets/css/main.css`、数据结构 `assets/css/typography.css`、`platform/portal.css`、`platform/components.css`、`platform/adapters.css`。均为增补性修改，删除新增内容即可回退（不影响既有正文与组件）。

---

## 第二轮扩展与字号/堆叠修复（2026-09-19）


### 交付

- 修复三科数学绘图引擎缺少 `Draw.G` 图元导致的 24 个页面组件报错（高等数学/线性代数/概率论的 `draw.js` 统一补充 `G` 并导出）。
- 字号/堆叠专项修复：
  - 六科 `G.label` 与 `Plot.note` 增加画布边界自适应（超宽标签自动缩小、越界锚点夹紧）；
  - 窄屏下六科可视化画布保持最小逻辑宽度并横向滚动（`.viz-body`），不再裁切文字；
  - 数据结构伪代码面板长语句自动换行，不再右侧裁切。
- 可视化内容 +50%：数据结构 35→54 个实验、计组 30→45、操作系统 15→23、计算机网络 20→30、高等数学 32→48、线性代数 20→30、概率论 61→94，全平台交互可视化 213→324，学习入口 221→240。

### 执行结果

| 检查 | 结果 |
| --- | --- |
| `npm test` | 2/2 通过（目录统计与发布白名单，数量断言已更新） |
| `npm run audit` | 七科 0 问题 |
| `npm run test:browser`（根） | 10/10 通过 |
| 数据结构 `npm test` | 64/64 通过 |
| 数据结构 `npm run test:browser` | 24/24 通过，含全部 54 个实验自动播放 |
| `node scripts/check-source.cjs <各科>`（源码直检） | 七科全部 0 问题页面 |
| `node scripts/verify-all.cjs`（发布产物全量） | 240/240 入口、0 失败页面、0 文本越界/重叠 |
| 画布文字越界扫描（390px/1440px） | 仅余旋转坐标轴标签（检测器换算假象），无真实越界 |
| `node scripts/verify-ui.cjs` | 18 张截图、44 个概率小节、0 错误 |

### 回退

新增内容集中在各科 `assets/js/widgets/*.js`、`content/ch*.js`（数据结构还包括 `assets/js/algorithms/extra-basics.js` 与 `content/extensions.js` 注册）；`draw.js` 只做了增补性修改（补 `G`、标签自适应），未改动既有绘图行为。删除 `dist/` 重新 `npm run build` 即可回退发布产物。

---

# 首期平台验收记录

日期：2026-09-17。

## 交付

已完成统一门户、七科登记、两科共享导航与样式、统一深浅主题、79 条真实学习入口搜索、最近访问、手机适配、静态发布脚本、Nginx / Compose 配置和扩展文档。其余五科为待建设状态，没有新增课程内容。

课程源数据及算法模块未修改。对子项目的功能调整集中在主题接入、移动目录的键盘焦点和概率论损坏进度回退。

## 执行结果

| 检查 | 结果 |
| --- | --- |
| `npm test`（根目录） | 2/2，通过真实目录统计、发布资源和白名单检查 |
| `npm run test:browser`（根目录） | 10/10，通过门户搜索、导航、主题、子路径、320/390px、最近访问、存储兼容、打印、键盘焦点 |
| 数据结构 `npm test` | 64/64 |
| 数据结构 `npm run test:browser` | 24/24，包括全部 35 实验自动播放 |
| `node scripts/verify-ui.cjs` | 18 张截图；44 个概率小节；页面异常、HTTP 错误、KaTeX 错误及页面宽度检查均未报错 |
| `docker compose config --quiet` | 退出码 0 |
| 本地发布预览 HTTP HEAD | 200 |

自动测试合计 100 项通过；概率论逐页检查与截图检查另计。概率论测试包含参数调整、切换主题保持参数、画布重绘、掌握标记刷新持久化；没有逐一审计全部数学结论或穷举每个图示的参数。

## 独立审查及修复

独立审查发现并复现两个 P2 问题，均已写失败测试后修复：

1. 两科移动目录的键盘访问：打开后焦点进入搜索，关闭后返回菜单，隐藏目录 inert，Tab 留在目录，Escape 关闭。
2. 概率论进度为 JSON null 时白屏：现在先校验对象结构，损坏数据回退到空状态。

最后视觉检查发现 320px 概率表格中的定位公式会扩大根页面宽度。给表格滚动容器建立定位上下文后，表格内部可滚动，根页面宽度恢复；已纳入集成回归。

## 视觉检查

本目录含门户、数据结构实验页和概率论内容页在 1440、390、320px 下的浅色/深色截图。人工查看了桌面门户浅色和深色、手机门户、桌面数据结构深色、手机概率论表格。机器结果见 `ui-results.json`。

## 环境限制

- 本机 Docker Desktop Linux 引擎未运行，`docker info` 返回管道不存在；未进行镜像构建、容器启动或服务器远程上线。仅 Compose 配置解析与静态产物经过本机验证。
- Playwright 在 Windows 上出现既有 Chrome 临时目录清理 EPERM 提示，不影响测试断言与退出码；未修改系统权限。
- 学习状态仍存于当前浏览器来源，不提供跨设备同步。

## 回退

原入口及应用脚本备份：`backups/platform-20260917-200527/`。恢复这四个文件可撤回两科共享层接入；新增平台文件独立于课程内容和算法源码。
