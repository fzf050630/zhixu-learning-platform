/* ============================================================
   ch3.js — 第3章 内存管理
   ============================================================ */
(function (global) {
  'use strict';
  global.CH3 = {
    id: 'ch3', no: '3', title: '内存管理',
    subtitle: '从连续分配到分页、分段，再到虚拟内存：用有限的物理内存支撑更大的地址空间。',
    tags: ['连续分配', '分页', '分段', '段页式', '虚拟内存', '请求分页', '页面置换', '缺页'],
    sections: [
      {
        id: 'ch3-s1', num: '3.1', title: '内存管理概念',
        lead: '内存管理负责分配回收、地址转换、保护共享与扩充；分页与分段是两种基本非连续分配方式。',
        blocks: [
          { t: 'h3', idx: '①', text: '内存管理的基本功能' },
          { t: 'list', items: [
            '<b>内存空间的分配与回收</b>；',
            '<b>地址转换</b>：逻辑地址（相对地址）→ 物理地址（绝对地址）；',
            '<b>内存空间的扩充</b>：覆盖、交换、虚拟存储；',
            '<b>内存保护与共享</b>：越界检查、访问权限控制。'
          ]},
          { t: 'card', kind: 'def', tag: '定义', title: '逻辑地址与物理地址', html:
            '<p class="tight"><b>逻辑地址</b>：程序经编译/链接后使用的地址，从 0 开始编号；<b>物理地址</b>：内存单元的实际地址。静态重定位在装入时转换，动态重定位在运行时由重定位寄存器转换。</p>'
          },
          { t: 'h3', idx: '②', text: '连续分配管理方式' },
          { t: 'viz', build: 'memAlloc', title: '连续分配与动态分区算法', sub: '切换首次/最佳/最坏适应' },
          { t: 'table', head: ['算法', '策略', '特点'], rows: [
            ['首次适应', '从头找第一个够大的空闲区', '简单、速度快，低地址易碎片化'],
            ['最佳适应', '找最小的够用空闲区', '留下小碎片最多'],
            ['最坏适应', '找最大的空闲区', '大分区被快速用完'],
            ['邻近适应', '从上次位置继续找', '可能使高地址大分区被切碎']
          ]},
          { t: 'algo', name: '动态分区分配算法', role: '连续分配 · 空闲分区选择', cost: '时间 O(n) 扫描空闲分区链 · 空间 O(n) 分区表/链',
            goal: '为新作业在空闲分区中选择一个分区：首次、最佳、最坏、邻近适应只在选择规则上不同。',
            inputs: '内存中有若干空闲分区（大小已知），作业提出大小为 request 的空间请求。',
            steps: [
              '首次适应：从低地址开始扫描，选择<b>第一个</b>大小足够的空闲分区进行分配。',
              '最佳适应：选择能满足请求且容量<b>最小</b>的空闲分区，尽可能少浪费大分区。',
              '最佳适应使空闲分区按容量递增排序，但会留下大量难以利用的小碎片。',
              '最坏适应：总是选择容量最大的空闲分区切一块给作业，分区按容量递减排序。',
              '邻近适应：从上一次分配结束的位置继续向后扫描（可循环），选第一个够大的分区。',
              '若分区被恰好用完则从空闲链中删除；有剩余则把剩余部分留在原位置或按序插入。',
              '回收时把释放分区与相邻空闲分区<b>合并</b>，再按所选算法的规则重新组织空闲链。'
            ],
            keys: [
              '四种算法分配与回收的框架相同，区别只在“选哪个空闲分区”。',
              '首次适应综合性能最好；最佳适应小碎片最多；最坏适应易耗尽大分区。',
              '邻近适应使高地址大分区也被切碎，通常效果最差。'
            ]
          },
          { t: 'h3', idx: '③', text: '基本分页 vs 基本分段' },
          { t: 'table', head: ['维度', '分页', '分段'], rows: [
            ['划分依据', '物理块大小固定', '逻辑意义（代码/数据/栈）'],
            ['大小', '页大小固定', '段长可变'],
            ['地址', '一维（页号+页内偏移）', '二维（段号+段内偏移）'],
            ['碎片', '仅少量页内碎片', '有外部碎片'],
            ['共享保护', '不便按逻辑共享', '便于按逻辑共享与保护']
          ]},
          { t: 'viz', build: 'paging', title: '分页 / 分段地址转换', sub: '输入逻辑地址，观察页表 / 段表' },
          { t: 'viz', build: 'addressTranslate', title: '分页地址变换详解', sub: '调节逻辑地址与页大小，修改当前页的页框号' },
          { t: 'card', kind: 'key', tag: '必记', title: '地址计算', html:
            '<div class="fml"><div class="fml-line">页号 = 逻辑地址 ÷ 页面大小　；　页内偏移 = 逻辑地址 mod 页面大小</div>' +
            '<div class="fml-line">物理地址 = 物理块号 × 页面大小 + 页内偏移</div></div>' +
            '<p class="tight">页大小为 2 的幂时，低位是页内偏移，高位是页号，用位运算直接切分。</p>'
          },
          { t: 'algo', name: '分页地址变换流程', role: '地址转换', cost: '时间：最多访存 2 次（查页表 + 取数据）· 空间 O(页数) 页表',
            goal: '把一维逻辑地址按页号与页内偏移拆开，查页表得到页框号并拼接出物理地址。',
            inputs: '逻辑地址 A 与页面大小 L（常为 2 的幂）；页表给出页号到页框号的映射。',
            steps: [
              '按页面大小拆分逻辑地址：页号 P = A ÷ L，页内偏移 W = A mod L。',
              '把页号 P 与页表长度比较，越界则产生<b>越界中断</b>并终止本次访问。',
              '以页号 P 为索引查页表（可先查快表 TLB，命中则直接取得页框号）。',
              '取出页表项中的页框号 b，并检查存取控制等权限信息。',
              '拼接物理地址：物理地址 = b × L + W。',
              '按物理地址访问内存完成取数或存数，本次地址变换结束。'
            ],
            keys: [
              '页面大小为 2 的幂时，页内偏移取逻辑地址的低位，页号取高位。',
              '页表项数与页号范围有关，页内偏移位数由页面大小决定。',
              '引入快表后命中时无需访存查页表，可把地址变换减少为一次访存。'
            ]
          },
          { t: 'card', kind: 'tip', tag: '段页式', title: '段页式存储管理', html:
            '<p class="tight">先按逻辑分段，再把每个段分页。地址结构 = 段号 + 页号 + 页内偏移；需要段表 + 页表两级查表，访问一次数据需多次访存（可用快表优化）。</p>'
          }
        ],
        examples: [
          {
            no: '例 3.1', meta: '分页地址',
            q: '页面大小 4 KB，逻辑地址 0x2A3C 的页号与页内偏移各是多少？若页表给出页框号 8，物理地址是多少？',
            sol: '<p>4 KB = 2¹²，页内偏移取低 12 位：0x2A3C & 0xFFF = <b>0xA3C</b>；页号 = 0x2A3C >> 12 = <b>0x2</b>。</p><p>物理地址 = 8 × 4096 + 0xA3C = 0x8000 + 0xA3C = <b>0x8A3C</b>。</p>'
          }
        ],
        pitfalls: [
          '分页对用户透明，分段对用户可见；段长可变，页大小固定。',
          '分段的段内偏移越界会产生越界中断；分页不会越界，但页号可能超出页表范围。'
        ]
      },
      {
        id: 'ch3-s2', num: '3.2', title: '虚拟内存管理',
        lead: '虚拟内存用“部分装入 + 按需调页 + 置换”让程序在小于其大小的内存中运行。',
        blocks: [
          { t: 'h3', idx: '①', text: '虚拟内存的概念与特征' },
          { t: 'card', kind: 'def', tag: '定义', title: '虚拟内存的三个特征', html:
            '<ul class="none"><li><b>多次性</b>：作业分多次调入内存；</li><li><b>对换性</b>：运行时可换入换出；</li><li><b>虚拟性</b>：逻辑上扩充内存容量。</li></ul><p class="tight">实现基础是<b>局部性原理</b>（时间局部性与空间局部性）。</p>'
          },
          { t: 'h3', idx: '②', text: '请求分页与页表项' },
          { t: 'table', head: ['字段', '作用'], rows: [
            ['页框号', '该页在内存中的物理块号'],
            ['有效位（存在位）', '该页是否已调入内存'],
            ['修改位（脏位）', '是否被写过，决定换出是否需写回磁盘'],
            ['访问位', '是否被访问过，供置换算法参考'],
            ['访问权限', '读/写/执行等保护信息'],
            ['外存地址', '该页在磁盘上的位置']
          ]},
          { t: 'h3', idx: '③', text: '页面置换算法' },
          { t: 'viz', build: 'pageReplace', title: '页面置换算法', sub: 'FIFO / LRU / OPT，观察缺页与置换' },
          { t: 'table', head: ['算法', '思想', '特点'], rows: [
            ['OPT', '替换未来最久不会使用的页', '缺页最少，但无法实现，作为理想基准'],
            ['FIFO', '替换最早进入的页', '简单，可能出现 Belady 异常'],
            ['LRU', '替换最久未使用的页', '性能接近 OPT，需硬件记录访问历史'],
            ['CLOCK', '环形扫描，访问位为 1 则给二次机会', 'LRU 的近似，开销小']
          ]},
          { t: 'algo', name: '最佳置换算法 OPT', role: '页面置换 · 理想基准', cost: '时间 O(n) 每次查找未来引用 · 空间 O(块数) 内存映像',
            goal: '淘汰以后永不使用、或在最长时间内不再被访问的页面，使缺页次数最少。',
            inputs: '已知整个页面访问串与物理块数；需要未来的访问信息，实际系统无法实现。',
            steps: [
              '访问页面时，若该页已在内存则命中，不产生置换。',
              '若缺页且有空闲物理块，直接把页面调入，并累计一次缺页。',
              '若缺页且无空闲块，则查看将来访问串，比较各驻留页面下次被访问的<b>时间</b>。',
              '选择以后最长时间不再使用（或永不再用）的页面淘汰。',
              '调入所需页面并更新内存中的页面集合，重复处理直至访问串结束。'
            ],
            keys: [
              'OPT 缺页率最低，是理论下界，实际无法实现、仅作比较基准。',
              'OPT 属于堆栈类算法，不会出现 Belady 异常。',
              '选择题比较 FIFO / LRU 优劣时，先把 OPT 的缺页次数算出作对照。'
            ]
          },
          { t: 'algo', name: '先进先出置换算法 FIFO', role: '页面置换', cost: '时间 O(1) 每次置换 · 空间 O(块数) 队列',
            goal: '淘汰最早进入内存的页面，用队列记录各页进入内存的先后顺序。',
            inputs: '页面访问串与物理块数；内存块按进入时间顺序排成一个队列。',
            steps: [
              '访问页面时若已在内存则命中，队列中页面的相对顺序不变。',
              '若缺页且有空闲块，把新页调入并把所在块加入<b>队尾</b>。',
              '若缺页且内存已满，淘汰队首（最早进入）的页面。',
              '调入新页并把它加入队尾，队列长度始终等于物理块数。',
              '重复处理直至访问串结束，统计缺页次数。'
            ],
            keys: [
              'FIFO 实现简单但性能差，不考虑页面是否常用、与访问规律不适应。',
              '可能产生 Belady 异常：物理块数增多，缺页次数反而增加。',
              'FIFO 不是堆栈类算法，命中率不随块数单调变化。'
            ]
          },
          { t: 'algo', name: '最近最久未使用 LRU', role: '页面置换', cost: '时间 O(1) 均摊更新 · 空间 O(块数) 时间戳/栈',
            goal: '淘汰最近最久未使用的页面：比较各页上次被访问的时间，最远者先淘汰。',
            inputs: '页面访问串与物理块数；需要用寄存器或栈记录每页最近一次被访问的时间。',
            steps: [
              '访问页面时若命中，则更新该页的时间戳，标记为<b>最近使用</b>。',
              '若缺页且有空闲块，直接把页面调入并标记为最近使用。',
              '若缺页且无空闲块，淘汰上次访问时间最早、最久未被使用的页面。',
              '调入新页并把该页标记为最近使用，内存中其余页保持原有次序。',
              '重复处理直至访问串结束，统计缺页次数。'
            ],
            keys: [
              'LRU 用“过去”预测“未来”，性能接近 OPT。',
              'LRU 是堆栈类算法，不会出现 Belady 异常。',
              '需要硬件支持（时间戳或栈），实现开销比 FIFO、CLOCK 大。'
            ]
          },
          { t: 'algo', name: '时钟置换算法 CLOCK（含改进型）', role: '页面置换 · LRU 近似', cost: '时间 O(1) 均摊（最多 4 轮扫描）· 空间 O(块数) 循环队列',
            goal: '用循环队列与访问位近似 LRU：访问位为 1 给第二次机会，为 0 则淘汰。',
            inputs: '内存块链成循环队列，每页设访问位 A，改进型另设修改位 M。',
            steps: [
              '页面被访问时把其访问位置 <b>A = 1</b>，指针指向下一个待检查的块。',
              '缺页需要置换时，检查指针所指页面的访问位 A。',
              '若 A = 1，则把该位清 0 作为“第二次机会”，指针前移继续检查。',
              '若 A = 0，则淘汰该页并调入新页，简单 CLOCK 最多扫描两轮。',
              '改进型 CLOCK 用（访问位 A，修改位 M）四轮扫描：第一轮淘汰 <b>(0, 0)</b> 页。',
              '第二轮淘汰 (0, 1) 页并把扫过的页访问位全部置 0；第三、四轮再按此序继续。',
              '因此改进型最多四轮扫描必选中一页，优先淘汰未访问且未修改的页面。'
            ],
            keys: [
              '简单 CLOCK 最多两轮选中淘汰页；改进型最多四轮。',
              '改进型优先淘汰既未访问又未修改的页，减少写回磁盘的 I/O 开销。',
              'CLOCK 是 LRU 的低开销近似，性能接近 LRU 但不如它精确。'
            ]
          },
          { t: 'viz', build: 'workingSet', title: '工作集与抖动', sub: '滑动窗口、工作集大小与缺页率曲线' },
          { t: 'card', kind: 'warn', tag: '注意', title: 'Belady 异常', html:
            '<p class="tight">FIFO 算法下，分配的物理块数增多，缺页次数反而可能增加，称为 Belady 异常。LRU 与 OPT 属于堆栈类算法，不会出现该异常。</p>'
          },
          { t: 'h3', idx: '④', text: '地址翻译与缺页处理' },
          { t: 'steps', items: [
            '按逻辑地址的页号查页表（先查 TLB）；',
            '有效位为 1 → 命中，形成物理地址访问；',
            '有效位为 0 → 缺页中断，若无空闲块则按置换算法淘汰一页；',
            '被淘汰页若被修改（脏位），需写回磁盘；',
            '从磁盘调入所需页，更新页表，重新执行被中断的指令。'
          ]},
          { t: 'card', kind: 'key', tag: '必记', title: '有效访问时间', html:
            '<div class="fml"><div class="fml-line">EAT = 命中率 × 命中时间 + 缺页率 × 缺页处理时间</div></div>' +
            '<p class="tight">缺页处理涉及磁盘 I/O，代价比内存访问高几个数量级，因此命中率对性能影响极大。</p>'
          }
        ],
        examples: [
          {
            no: '例 3.2', meta: '缺页率',
            q: '某请求分页系统访问 1000 次，缺页 20 次，内存访问时间 100 ns，缺页处理时间 8 ms。求有效访问时间。',
            sol: '<p>命中率 = 980/1000 = 0.98，缺页率 = 0.02。</p><p>EAT ≈ 0.98 × 100 ns + 0.02 × 8 ms = 98 ns + 160000 ns = <b>约 160.1 µs</b>。可见 2% 的缺页率也会使平均访问时间大幅上升。</p>'
          }
        ],
        pitfalls: [
          '缺页中断属于内中断（异常），处理完后要重新执行被中断的指令。',
          'BELADY 异常只出现在 FIFO 等非堆栈类算法中。',
          '页面大小影响命中率：太小增加页表开销与缺页，太大增加页内碎片。'
        ]
      }
    ]
  };
})(window);
