/* ============================================================
   ch4.js — 第4章 指令系统
   ============================================================ */
(function (global) {
  'use strict';
  global.CH4 = {
    id: 'ch4', no: '4', title: '指令系统',
    subtitle: '指令是软件与硬件的接口：格式决定编码，寻址决定操作数从哪来。',
    tags: ['指令格式', '操作码', '扩展操作码', '寻址方式', '有效地址', '汇编', 'CISC', 'RISC'],
    sections: [
      {
        id: 'ch4-s1', num: '4.1', title: '指令格式与操作码',
        lead: '一条指令 = 操作码 + 地址码；地址码的个数决定指令是几地址指令。',
        blocks: [
          { t: 'h3', idx: '①', text: '指令的基本格式' },
          { t: 'card', kind: 'def', tag: '定义', title: '操作码与地址码', html:
            '<p class="tight"><b>操作码</b>指出指令要做什么（运算、传送、转移等）；<b>地址码</b>指出操作数或结果存放的位置。按地址码个数分为零/一/二/三/四地址指令。</p>'
          },
          { t: 'viz', build: 'instrFormat', title: '指令格式与字段划分', sub: '调整字长、操作码位数与地址码个数' },
          { t: 'table', head: ['指令类型', '地址码', '典型含义'], rows: [
            ['零地址', '0', '如空操作、停机、堆栈运算'],
            ['一地址', '1', '如自增、取反，另一个操作数隐含在 ACC'],
            ['二地址', '2', 'A1 op A2，最常见'],
            ['三地址', '3', 'A3 ← A1 op A2'],
            ['四地址', '4', '再加下条指令地址']
          ]},
          { t: 'h3', idx: '②', text: '定长操作码' },
          { t: 'fml', rows: [
            { line: '操作码位数 n → 最多 2ⁿ 条指令', note: '译码简单、硬件规整' },
            { line: '指令字长固定，各字段位置固定', note: '' }
          ]},
          { t: 'card', kind: 'key', tag: '必记', title: '操作码与地址码的权衡', html:
            '<p class="tight">操作码越长，可表示的指令越多，但留给地址码的位数越少，寻址范围越小。定长操作码简单但浪费编码空间；扩展操作码（下一节）用不等长操作码提高利用率。</p>'
          }
        ],
        examples: [
          {
            no: '例 4.1', meta: '格式计算',
            q: '指令字长 16 位、4 位操作码、二地址指令，每地址码多少位？可寻址多少个单元？',
            sol: '<p>地址码总位数 = 16 − 4 = 12，每地址码 = 12 ÷ 2 = <b>6 位</b>，可寻址 2⁶ = <b>64 个单元</b>；操作码可表示 2⁴ = 16 条指令。</p>'
          }
        ],
        pitfalls: [
          '地址码位数不一定能整除，若不能整除则会浪费位数或需变长指令。',
          '操作码位数决定指令种类数，不是越大越好。'
        ]
      },
      {
        id: 'ch4-s2', num: '4.2', title: '寻址方式',
        lead: '寻址方式决定有效地址 EA 怎么算——这是指令系统最灵活、最常考的部分。',
        blocks: [
          { t: 'h3', idx: '①', text: '常见数据寻址方式' },
          { t: 'table', head: ['方式', 'EA 计算', '访存次数', '用途'], rows: [
            ['立即寻址', '形式地址 A 即操作数', '0', '常量'],
            ['直接寻址', 'EA = A', '1', '简单变量'],
            ['间接寻址', 'EA = (A)', '2', '指针'],
            ['寄存器寻址', 'EA = R', '0', '频繁使用的变量'],
            ['寄存器间接', 'EA = (R)', '1', '指针'],
            ['相对寻址', 'EA = PC + A', '1', '转移、程序浮动'],
            ['基址寻址', 'EA = BR + A', '1', '重定位、多道程序'],
            ['变址寻址', 'EA = IX + A', '1', '数组、循环']
          ]},
          { t: 'algo', name: '有效地址 EA 的计算流程', role: '寻址方式', cost: '按访存次数：0 次（立即 / 寄存器）· 1 次（直接、相对、基址、变址）· 2 次（间接）',
            goal: '按寻址方式字段对形式地址 A 与各寄存器作不同组合，算出有效地址 EA 并取出操作数。',
            inputs: '指令中的形式地址 A、寻址方式标志，以及 PC、基址寄存器 BR、变址寄存器 IX 与内存内容。',
            steps: [
              '译码寻址方式字段，确定本次 EA 的计算公式与访存次数。',
              '立即寻址<b>不计算地址</b>，形式地址 A 本身就是操作数。',
              '直接寻址取 EA = A；间接寻址先访存取 (A) 作为 EA，需两次访存。',
              '寄存器寻址取 EA = R，操作数在寄存器内不访存；寄存器间接寻址 EA = (R)，访存一次。',
              '相对寻址计算 EA = <b>PC + A</b>，A 为补码偏移、PC 已更新到下条指令地址。',
              '基址寻址 EA = BR + A（BR 由系统设定，用于重定位）；变址寻址 EA = IX + A（IX 由用户修改，用于数组）。',
              '复合寻址按<b>由内到外</b>的顺序套用上述公式，再按最终 EA 访存取操作数。'
            ],
            keys: [
              '立即与寄存器寻址都不访存，区别是操作数来自指令还是寄存器。',
              '相对寻址的基准是已加过指令长度的 PC，A 是补码偏移、可正可负。',
              '基址与变址都是「寄存器 + A」，区别在谁可变、面向系统还是面向用户。'
            ]
          },
          { t: 'viz', build: 'addressingMode', title: '寻址方式与有效地址', sub: '切换方式，观察 EA 与操作数的来源' },
          { t: 'h3', idx: '②', text: '基址与变址的区别' },
          { t: 'card', kind: 'tip', tag: '对比', title: '基址 vs 变址', html:
            '<ul class="none">' +
            '<li><b>基址寻址</b>：基址寄存器由系统/操作系统设定，程序执行中一般不变；形式地址 A 可变，用于把程序整体重定位。面向系统。</li>' +
            '<li><b>变址寻址</b>：变址寄存器由用户设定并可改变（如循环计数），形式地址 A 作为基地址；适合遍历数组。面向用户。</li>' +
            '</ul>'
          },
          { t: 'h3', idx: '③', text: '复合寻址' },
          { t: 'list', items: [
            '<b>相对间接</b>、<b>基址变址</b>等复合方式，把多种计算组合；',
            '寻址方式越多，指令越灵活，但译码与硬件越复杂。'
          ]}
        ],
        examples: [
          {
            no: '例 4.2', meta: 'EA 计算',
            q: '某指令采用变址寻址，变址寄存器内容 0x0800，形式地址 A = 0x0030。有效地址是多少？若采用间接寻址且形式地址为 0x30、内存 0x30 处存 0x2000，EA 又是多少？',
            sol: '<p>变址：EA = 0x0800 + 0x0030 = <b>0x0830</b>。</p><p>间接：EA = (0x30) = <b>0x2000</b>。</p>'
          }
        ],
        pitfalls: [
          '立即寻址不访存，操作数就在指令中。',
          '间接寻址需两次访存，速度慢但一次可扩大寻址范围。',
          '相对寻址的 A 通常是补码偏移量，可正可负。'
        ]
      },
      {
        id: 'ch4-s3', num: '4.3', title: '指令类型与扩展操作码',
        lead: '用不等长操作码把短码的“空闲编码”扩展成更多长码指令，提高编码利用率。',
        blocks: [
          { t: 'h3', idx: '①', text: '指令类型' },
          { t: 'table', head: ['类型', '举例'], rows: [
            ['数据传送', 'MOV、LOAD、STORE、PUSH、POP'],
            ['算术运算', 'ADD、SUB、MUL、DIV、INC、DEC'],
            ['逻辑运算', 'AND、OR、NOT、XOR、TEST'],
            ['移位操作', 'SHL、SHR、ROL、ROR'],
            ['转移指令', 'JMP、JZ、JC、CALL、RET'],
            ['输入输出', 'IN、OUT'],
            ['其他', 'NOP、HLT、中断指令']
          ]},
          { t: 'viz', build: 'expOpcode', title: '扩展操作码', sub: '调整短码指令数，观察可扩展空间' },
          { t: 'h3', idx: '②', text: '扩展操作码的原则' },
          { t: 'steps', items: [
            '短操作码指令占用一部分编码；',
            '剩余编码作为“扩展标志”，配合更长的操作码字段表示更多指令；',
            '关键约束：<b>短码不能是任何长码的前缀</b>，否则译码歧义；',
            '设计时按指令使用频率分配码长：常用指令用短码，不常用用长码。'
          ]},
          { t: 'card', kind: 'key', tag: '必记', title: '经典结论', html:
            '<p class="tight">设操作码固定为 n 位、地址码共 m 位，若前 k 种短码指令被占用，则可扩展出的长码指令数 = (2ⁿ − k) × 2^(地址码减少的位数)。每次扩展要“让出”一部分地址码位作为新的操作码位。</p>'
          },
          { t: 'algo', name: '扩展操作码的编码设计', role: '指令格式设计', cost: '时间 O(1) 设计计算 · 编码利用率高于定长操作码',
            goal: '让短码指令占用部分编码、剩余编码充当扩展前缀，在不增加指令字长的前提下表示更多指令。',
            inputs: '指令字长、操作码与地址码的位数划分，以及各类指令的数量需求与使用频率。',
            steps: [
              '确定地址码结构：每次扩展都要让出若干地址码位，<b>地址码减少</b>才能给操作码腾位。',
              '统计短码指令条数 k（操作码 n 位），检查它占用了 2ⁿ 个编码中的哪 k 个。',
              '把剩余 <b>(2ⁿ − k)</b> 个编码作为前缀，每个前缀再接 m 位扩展位，得到 (2ⁿ − k) × 2ᵐ 条长码指令。',
              '若还需更长的指令，继续保留部分编码作为前缀，按「逐级让位」的方式向下扩展。',
              '检验前缀约束：已用的短码<b>不能作为任何长码的前缀</b>，否则译码出现歧义。',
              '按使用频率分配码长：常用指令用短码、少用指令用长码，兼顾条数与译码效率。'
            ],
            keys: [
              '扩展的本质是「短码富余编码 + 地址码让位」，每扩展一级地址码位数就减少若干位。',
              '每次扩展至少要保留一个编码留给更长指令，否则无法继续向下扩展。',
              '短码用掉 k 个编码后，剩余 (2ⁿ − k) 个前缀各可扩展 2ᵐ 条长码指令。'
            ]
          }
        ],
        examples: [
          {
            no: '例 4.3', meta: '扩展操作码',
            q: '指令字长 16 位，采用扩展操作码：三地址指令 4 位操作码 + 3×4 位地址码；二地址指令操作码扩展为 8 位 + 2×4 位地址码。问二地址指令最多多少条？',
            sol: '<p>4 位操作码共 16 种，三地址指令占用若干种后，剩余编码作为扩展前缀。若三地址用了 k 条，则剩 (16−k) 个前缀，每个前缀再用后 4 位扩展出 16 种，故二地址最多 = (16−k)×16 条。若三地址用 4 条，则二地址最多 <b>12×16 = 192 条</b>。</p>'
          }
        ],
        pitfalls: [
          '扩展操作码下短码不能是长码前缀，否则无法唯一译码。',
          '计算时注意每次扩展“借用”了几位地址码。'
        ]
      },
      {
        id: 'ch4-s4', num: '4.4', title: '程序的机器级代码表示',
        lead: '选择、循环、过程调用在汇编层的固定套路：比较、条件跳转、栈传参。',
        blocks: [
          { t: 'h3', idx: '①', text: '常用汇编指令与寄存器' },
          { t: 'table', head: ['类别', '示例', '说明'], rows: [
            ['传送', 'mov a, b', 'b → a'],
            ['运算', 'add / sub / imul', '加减乘，结果影响标志位'],
            ['比较', 'cmp a, b', '做 a−b 但不保存结果，只置标志'],
            ['跳转', 'jmp / je / jg / js', '无条件 / 相等 / 大于 / 符号'],
            ['栈操作', 'push / pop / call / ret', '过程调用与返回'],
            ['寄存器', 'eax、ebx、esp、ebp、eip', '通用、栈指针、帧指针、指令指针']
          ]},
          { t: 'h3', idx: '②', text: '三种结构的机器级表示' },
          { t: 'card', kind: 'def', tag: '选择', title: 'if-else', html:
            '<pre class="code-block">cmp  eax, ebx\n<span class="cm">// eax &gt; ebx ? 取 eax : 取 ebx</span>\njle  L1        <span class="cm">// 若不大于则跳去 else 分支</span>\nmov  ecx, eax\njmp  L2\nL1:  mov  ecx, ebx\nL2:  ...</pre>'
          },
          { t: 'card', kind: 'def', tag: '循环', title: 'for 循环', html:
            '<pre class="code-block">mov  ecx, n      <span class="cm">// 循环计数</span>\nmov  eax, 0\nL1:  add  eax, ecx\n     dec  ecx      <span class="cm">// 计数减 1</span>\n     jnz  L1       <span class="cm">// 非 0 继续</span></pre>'
          },
          { t: 'card', kind: 'def', tag: '调用', title: '过程调用', html:
            '<ul class="none"><li><b>call</b>：把返回地址压栈，跳转到过程入口；</li><li><b>ret</b>：从栈弹出返回地址，跳回调用处；</li><li>参数可经寄存器或栈传递，<b>ebp</b> 建立栈帧，恢复现场后返回。</li></ul>'
          },
          { t: 'viz', build: 'stackFrame', title: '过程调用的栈帧', sub: '单步观察 call / ret 时参数、返回地址、旧 rbp 与局部变量的栈位置' },
          { t: 'card', kind: 'tip', tag: '提示', title: 'x86 与 MIPS', html:
            '<p class="tight">408 中常见的是 x86 风格的 AT&amp;T / Intel 汇编；MIPS 是典型的 RISC，指令定长，load/store 风格，只有访存指令能访问内存。</p>'
          }
        ],
                  examples: [
          {
            no: '例 4.4', meta: '基础 · 汇编语义',
            q: '执行 <code>cmp eax, ebx</code> 后跟 <code>jg L1</code>，说明其含义。',
            sol: '<p><code>cmp</code> 做 \( eax-ebx \) 但不保存结果，只置标志位；<code>jg</code> 是“大于则跳转”，判断的是<b>有符号比较</b>（结合 SF、OF、ZF），跳转成立时执行 L1 处指令，否则顺序执行下一条。</p>'
          },
          {
            no: '例 4.5', meta: '综合 · 循环翻译',
            q: '把 <code>for (i=0; i<n; i++) sum += a[i];</code> 的循环控制部分写成汇编要点。',
            sol: '<p>用寄存器分别承担：i（计数器）、n（上界）、sum（累加）、基址（数组首址）。</p><p>循环体：比较 i 与 n → 条件跳转退出 → 按 i 偏移取 a[i] 加到 sum → i 自增 → 无条件跳回循环头。</p><p>要点：数组访问依靠“基址 + 变址 × 比例”的寻址方式。</p>'
          }
        ],
        pitfalls: [
          'cmp 不改变操作数，只影响标志位。',
          '条件跳转依据的是标志位（ZF、SF、OF），不是直接的数值比较。',
          '过程调用必须保证栈平衡：压入与弹出成对。'
        ]
      },
      {
        id: 'ch4-s5', num: '4.5', title: 'CISC 与 RISC',
        lead: '一种用复杂指令完成更多工作，一种用简单指令配合编译器与流水线。',
        blocks: [
          { t: 'h3', idx: '①', text: '对比' },
          { t: 'table', head: ['维度', 'CISC', 'RISC'], rows: [
            ['指令数量', '多，几百条', '少，几十~一两百条'],
            ['指令长度', '变长', '定长'],
            ['寻址方式', '多且复杂', '少而简单'],
            ['访存', '多数指令可访存', '只有 load/store 访存'],
            ['通用寄存器', '较少', '多'],
            ['控制器', '微程序为主', '硬布线为主'],
            ['流水线', '较难', '易于实现'],
            ['典型', 'x86', 'MIPS、ARM']
          ]},
          { t: 'card', kind: 'key', tag: '必记', title: 'RISC 的设计哲学', html:
            '<ul class="none">' +
            '<li>指令定长、格式规整，便于流水线；</li>' +
            '<li>只有 load/store 访问内存，其余都在寄存器间进行；</li>' +
            '<li>大量通用寄存器减少访存；</li>' +
            '<li>用编译优化代替硬件的复杂指令。</li>' +
            '</ul>'
          },
          { t: 'card', kind: 'tip', tag: '趋势', title: '界限在模糊', html:
            '<p class="tight">现代 x86 内部把复杂指令翻译成类似 RISC 的微操作执行；ARM 也在增加指令。CISC 与 RISC 更多是设计取向而非绝对划分。</p>'
          }
        ],
                  examples: [
          {
            no: '例 4.6', meta: '基础 · CISC 与 RISC',
            q: '同一程序在 CISC 机上需 200 条指令、平均 CPI=3；在 RISC 机上需 300 条指令、平均 CPI=1.2。主频相同时哪台更快？',
            sol: '<p>CISC 总周期 = 200×3 = <b>600</b>；RISC 总周期 = 300×1.2 = <b>360</b>。</p><p>主频相同时 RISC 更快（360 < 600）。说明指令条数多并不等于慢，要看<b>总时钟周期数</b>。</p>'
          },
          {
            no: '例 4.7', meta: '综合 · 访存限制',
            q: '为什么 RISC 规定只有 load/store 指令能访问存储器？',
            sol: '<p>使指令定长、格式规整，便于流水线译码与执行；把访存集中到少数指令，简化数据通路与冒险判断；其余运算都在寄存器间进行，配合大量通用寄存器减少访存次数。</p>'
          }
        ],
        pitfalls: [
          'RISC 并非“指令越简单越慢”，其优势在于流水线与编译优化。',
          '“指令条数少”不等于执行快，要看 CPI 与总周期数。'
        ]
      }
    ]
  };
})(window);
