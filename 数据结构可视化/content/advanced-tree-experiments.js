(function(root,factory){const node=typeof module==='object'&&module.exports;const api=factory(node?require('../assets/js/algorithms/advanced-trees.js'):root.DS.Algorithms.advancedTrees,node?require('./experiment.js'):root.DS.Experiments);if(node)module.exports=api;root.DS=root.DS||{};root.DS.AdvancedTreeExperiments=api;})(globalThis,function(A,{defineExperiment}){
  const defaultValue='10, 20, 30, 15, 25, 5, 1, 8, 12, 18, 22, 28';
  function parse(raw){const text=typeof raw==='string'?raw:String(raw&&raw.values||'');const tokens=text.trim().split(/[\s,，]+/).filter(Boolean);if(!tokens.length||tokens.length>12||tokens.some(x=>!/^[-+]?\d+$/.test(x)||Math.abs(Number(x))>999))return {ok:false,message:'请输入 1–12 个 [-999,999] 整数，以空格或逗号分隔。'};const query=typeof raw==='object'&&raw?String(raw.target??'').trim():'';if(query&&(!/^[-+]?\d+$/.test(query)||Math.abs(Number(query))>999))return {ok:false,message:'查找值必须是 [-999,999] 整数，也可留空。'};return {ok:true,value:{values:tokens.map(Number),target:query?Number(query):undefined}};}
  function describeState(s){return [{title:'结构与链接（重复键拒绝；修复中的临时状态由阶段标识）',columns:['结点','关键字','颜色 / 类型','父结点','孩子','next'],rows:Object.values(s.tree.nodes).map(n=>[n.id,n.keys.join(', '),n.color||(n.leaf?'叶子':'索引'),n.parent||'—',n.children.map(x=>x||'NIL').join(', ')||'—',n.next||'—'])},{title:'当前阶段',columns:['阶段','查找结果'],rows:[[s.phase,s.found===false?'不存在':s.found||'—']]}];}
  const specs=[['red-black-tree','红黑树插入与查找','redBlack','旋转与变色'],['b-tree','B 树插入与查找','bTree','t=2 分裂提升'],['b-plus-tree','B+ 树插入与查找','bPlus','叶链与分隔键']];
  return specs.map(([id,title,method,tag])=>defineExperiment({id,chapter:'search',title,tag,difficulty:'进阶',visualizer:'multi-tree',generator:A[method],preset:()=>[[10,20,30,15,25,5,1,8,12,18,22,28],22],input:{type:'multi',label:'插入序列与查找值',placeholder:defaultValue,defaultValue,hint:'重复键明确拒绝；查找值可留空。B 树 t=2，每结点最多 3 键；B+ 树最多 3 键 / 4 孩子，记录仅存于叶子。',fields:[{name:'values',label:'插入序列（1–12 个整数；重复键拒绝）',defaultValue},{name:'target',label:'查找值（可留空）',defaultValue:'22'}],parse},inputAdapter:input=>[input.values,input.target],describeState,
    explain:method==='redBlack'?{goal:'在二叉排序树规则下插入红结点，并通过变色与旋转保持红黑树五条性质。',inputs:'插入序列（1–12 个整数，重复键拒绝）与可选的查找值；空指针视为黑 NIL。',steps:['初始化红黑树：空树，所有 NIL 叶结点约定为黑色。','插入前先在树中查重：沿排序树比较关键字，若已存在则拒绝该重复键。','从根开始按关键字大小下降到插入位置，把新结点作为<b>红色</b>结点挂在父结点下。','若父结点为黑色，红黑性质未被破坏，本轮插入结束。','若父结点为红色，查看叔结点颜色：叔结点为红时把父、叔涂黑、祖父涂红，再把祖父作为当前结点继续向上修复。','叔结点为黑时按形态旋转：LL 或 RR 做一次旋转，LR 或 RL 先旋转成同向再做一次；旋转后新子树的根涂黑、两个孩子涂红。','修复逐层上移，最后强制根结点为黑色，并校验黑高相等、红结点孩子为黑等不变量。','建树完成后执行查找：在访问到的每个结点内比较目标键，命中返回该结点，走到 NIL 则返回 NOT_FOUND。'],keys:['新插入结点一律为红色；只有父结点也为红时才需要修复，修复终止于父结点为黑或到达根。','叔红变色、叔黑旋转：旋转后必须把新子树的根涂黑、两个孩子涂红，否则黑高会被破坏。','根始终重新涂黑，任意结点到 NIL 的黑结点数相等；查找、插入、删除均为 O(log n)。'],cost:'时间 O(log n) · 空间 O(1)（旋转与变色）'}:method==='bTree'?{goal:'在 t=2 的 B 树中插入关键字，通过分裂满结点保持多路平衡查找性质。',inputs:'插入序列（1–12 个整数，重复键拒绝）与可选的查找值；t=2，每结点最多 3 键。',steps:['初始化 t=2 的 B 树：每个结点最多 2t−1=3 个关键字，根以外至少 t−1=1 个，所有叶结点同层。','插入前先查重：若关键字已存在则拒绝，不插入重复键。','从根开始按关键字大小比较，选择对应的子树逐层下降，定位叶结点插入位置。','下降途中若即将进入的孩子结点已满（已有 3 个关键字），先分裂它：中间关键字提升到父结点，左右两半各成新结点，再继续下降。','若根结点已满，先分裂根，中间关键字成为新根，树高增加一层。','到达叶结点后把关键字插入合适位置，保持结点内关键字有序。','插入后若结点关键字数达到上限 3 需要分裂，则继续把中间关键字提升，必要时沿祖先链向上分裂。','全部插入结束后所有叶仍在同一层；查找时在结点内比较，命中返回结点，否则沿相应子树下降，到叶仍未命中则返回 NOT_FOUND。'],keys:['B 树的绝对平衡：所有叶结点等深；除根外每个结点关键字数为 t−1 到 2t−1。','分裂先把中间关键字提升到父结点，再以它为界把剩余键分成左右两半；根分裂是树长高的唯一途径。','插入都发生在叶结点层；含 k 个关键字的非叶结点有 k+1 棵子树，比较后进入对应子树。','时间 O(log n)，与树高同阶；结点容量大，可显著减少磁盘 I/O 次数。'],cost:'时间 O(log n) · 空间 O(n)'}:{goal:'在 B+ 树中插入关键字，通过分裂与叶链维护保持索引与叶结点有序。',inputs:'插入序列（1–12 个整数，重复键拒绝）与可选的查找值；每结点最多 3 键 / 4 孩子。',steps:['初始化 B+ 树：每结点最多 3 个关键字、4 棵子树；记录只存放在叶结点，非叶结点仅作索引。','插入前先查重：若关键字已存在则拒绝，不插入重复键。','从根开始按非叶结点中的分隔键比较，选择目标键所属的子树一路下降到叶结点。','把关键字插入目标叶并保持叶内有序；若叶中最小值变化，同步刷新祖先结点的分隔键。','若叶结点关键字数超过上限 3 发生溢出，把叶分裂为左右两半。','取右叶的最小键复制到父结点作为新分隔键（记录本身仍留在叶中），并调整父结点的孩子指针。','用 next 指针把新右叶链到原叶之后，保持全部叶结点按键有序、可从最左叶顺序扫描整条叶链。','非叶结点若也超出上限则同样分裂并向上提升分隔键，最坏裂到根使树高增加一层。','插入结束后所有叶仍在同一层；查找时沿分隔键下降到叶，在叶内比较目标，命中返回，否则返回 NOT_FOUND。'],keys:['B+ 树分裂时提升的是右叶最小键的副本，仅作分隔键；B 树提升的是真实关键字。','叶结点用 next 指针串成有序链表，适合范围查询与顺序扫描，B 树没有叶链。','非叶结点只存索引键，每次查找都必须走到叶结点，各条查找路径长度相同。','上限 3 键 / 4 孩子时叶分裂后左右各约一半键；查找、插入均为 O(log n)。'],cost:'时间 O(log n) · 空间 O(n)'},
    code:[
    method==='redBlack'?'initialize RB tree; NIL is BLACK;':method==='bTree'?'initialize B tree; minimum degree t = 2;':'initialize B+ tree; maximum keys = 3;',
    'for key in values: reject duplicate keys;',
    '  compare keys and descend to insertion position;',
    method==='redBlack'?'  attach a RED node;':method==='bTree'?'  split full children before descending; insert into leaf;':'  insert record in leaf; refresh separator minima;',
    method==='redBlack'?'  while parent is RED: recolor parent/uncle/grandparent;':'  detect full or overflowing node;',
    method==='redBlack'?'  rotate when uncle is BLACK; reconnect parents;':method==='bTree'?'  split 3-key child; promote middle key;':'  split overflow; link leaves; copy right minimum;',
    method==='redBlack'?'  root.color = BLACK; verify invariants;':'  finish insertion; verify balanced leaf depth;',
    'finish building the ordered tree;',
    method==='bPlus'?'search(target): follow separators to a LEAF;':'search(target): compare in each visited node;',
    'return found node, or NOT_FOUND;'
  ]}));
});
