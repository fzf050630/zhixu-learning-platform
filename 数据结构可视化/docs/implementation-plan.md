# 架构优化实施记录 · 2026-09-16

按用户给定设计和优先级执行，保持 classic scripts + UMD，支持 file:// 离线打开。

1. LabRuntime：统一配置/快照校验、播放器创建销毁、异常隔离、Canvas/代码/说明同步、按钮与键盘事件；页面仅负责路由和外壳。补生命周期、错误恢复、结束通知测试。
2. CanvasScene：集中 DPI、主题、文本、箭头、方框等图元。八种渲染器独立注册，修复深色背景及图节点指标显示。
3. 内容：六个章节清单，统一 defineExperiment 接口，集中声明预设输入、算法生成器、代码、标签和难度；总表检查 ID 与章节关联。
4. Chrome：Playwright 仅作开发依赖；使用 file:// 覆盖所有实验完整自动播放、路由切换、按钮、主题、字体、响应式、错误恢复和控制台。
5. 学习体验：本轮完善代码排版/语法配色、键盘作用域、移动布局和减弱动画；任意图树编辑器及新算法作为后续独立扩展。

验证：node --test tests/*.test.js；npm run test:browser。保留 26 个现有实验的算法语义。

完成验证：30/30 Node 测试、8/8 Chrome 场景通过；浏览器覆盖全部 26 个实验完整自动播放，命令退出码为 0。已查看桌面、手机、深色 2× DPI 截图。

环境记录：Windows Chrome 临时配置目录存在 EPERM 清理限制，已通过测试专用的有限重试处理避免退出挂起，残留路径会在控制台明确报告；详见 README。
