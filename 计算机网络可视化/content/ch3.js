/* ============================================================
   ch3.js — 第3章 数据链路层
   ============================================================ */
(function (global) {
  'use strict';
  global.CH3 = {
    id: 'ch3', no: '3', title: '数据链路层',
    subtitle: '把不可靠的物理链路变成无差错的数据链路：组帧、差错控制、流量控制、介质访问控制。',
    tags: ['封装成帧', '透明传输', 'CRC', '滑动窗口', 'GBN', 'SR', 'CSMA/CD', '以太网', 'MAC', '交换机'],
    sections: [
      {
        id: 'ch3-s1', num: '3.1', title: '数据链路层功能与组帧',
        lead: '链路层在物理层之上提供帧的传输；帧定界与透明传输是可靠传输的前提。',
        blocks: [
          { t: 'h3', idx: '①', text: '数据链路层的功能' },
          { t: 'list', items: [
            '链路管理（建立、维持、释放）；',
            '<b>封装成帧</b>：在数据前后加首部与尾部，形成帧；',
            '<b>透明传输</b>：无论数据是什么比特组合，都能原样传输；',
            '<b>差错控制</b>：检错/纠错；',
            '<b>流量控制</b>：协调收发速率。'
          ]},
          { t: 'h3', idx: '②', text: '组帧方法' },
          { t: 'viz', build: 'framing', title: '零比特填充与透明传输', sub: '输入数据，观察填充过程' },
          { t: 'table', head: ['方法', '思想'], rows: [
            ['字符计数法', '首部用一个计数字段表示帧长，出错后难以恢复'],
            ['字节填充法', '用标志字节 F 定界，数据中出现 F 或 ESC 时插入转义字节'],
            ['零比特填充法', '标志为 01111110，数据中连续 5 个 1 后插 0'],
            ['违规编码法', '利用编码中不会出现的组合作为帧定界']
          ]},
          { t: 'algo', name: '字节填充法（转义）', role: '面向字节的透明传输', cost: '标志 7E · 转义 7D · 被转义字节异或 20H',
            goal: '用转义字节标记数据中出现的帧标志与转义字符，使任意数据都能原样传输。',
            inputs: '以字节为单位的帧数据；双方约定帧标志为 7E、转义字符为 7D。',
            steps: [
              '发送前扫描数据字节，找出其中出现的<b>帧标志 7E 与转义字符 7D</b>。',
              '若数据中出现 7E，把它改写为 <b>7D 5E</b>（即 7E 与 20H 异或）。',
              '若数据中出现 7D，把它改写为 <b>7D 5D</b>（即 7D 与 20H 异或）。',
              '在改写后的数据首尾各加一个帧标志 7E，接收方按 7E 定界帧的起止。',
              '接收方扫描到 7D 时删除它，并把其后字节与 <b>20H</b> 异或，还原出原字节。',
              '核对还原结果与发送数据一致，确认数据中的 7E 不会被误认为帧标志。'
            ],
            keys: [
              '转义规则：7E → 7D 5E、7D → 7D 5D，都是“加 7D 后与原字节异或 20H”。',
              '接收方一见 7D 就删除并异或 20H，规则与发送方严格对称。',
              '填充只作用于帧的数据部分，帧标志本身不参与转义。'
            ]
          },
          { t: 'algo', name: '零比特填充法', role: '面向比特的透明传输（HDLC / PPP）', cost: '帧标志 01111110 · 连续 5 个 1 后插 0',
            goal: '在数据中连续 5 个 1 之后插入一个 0，使数据永远不会出现帧标志 01111110。',
            inputs: '任意待发送的比特序列；双方约定帧标志为 01111110。',
            steps: [
              '发送方扫描数据比特流，统计当前连续出现的 <b>1</b> 的个数。',
              '每当已连续出现 5 个 1，无论下一位是什么，都紧跟插入一个 <b>0</b>。',
              '继续扫描剩余比特并重复插入，直到数据部分全部处理完。',
              '在填充后的比特流首尾各加一个帧标志 01111110，供接收方定界。',
              '接收方扫描帧内比特，每遇到连续 5 个 1 就<b>删除其后的一个 0</b>。',
              '去掉全部填充位后得到原始数据；数据中最多只有 5 个连续 1，标志不会与数据混淆。'
            ],
            keys: [
              '发送方“见 5 个 1 就插 0”，接收方“见 5 个 1 就删其后的 0”，二者严格对称。',
              '填充后数据不会出现 01111110，因此帧标志可以安全地用于定界。',
              'HDLC、PPP 等面向比特的协议采用此方法，填充位不计入数据长度。'
            ]
          },
          { t: 'card', kind: 'key', tag: '必记', title: '透明传输的本质', html:
            '<p class="tight">透明传输就是让数据中出现的任何比特组合都不会被误认为帧定界符。零比特填充（HDLC/PPP 常用）与字节填充（面向字节协议）都是为此服务。</p>'
          }
        ],
        examples: [
          {
            no: '例 3.3', meta: '基础 · 零比特填充',
            q: 'HDLC 采用零比特填充法，帧标志为 <code>01111110</code>。要发送的数据比特串为 <code>0110111110111110</code>，写出填充后实际发送的比特串，并说明接收方如何还原。',
            sol: '<p>发送方在数据中每出现连续 5 个 1 之后就插入一个 0：<code>0110</code> 后遇 <code>11111</code> 插 0，接着遇 <code>11111</code> 再插 0，末尾 <code>0</code> 保留。</p><p>填充后为 <b>011011111001111100</b>。接收方每遇到连续 5 个 1 就删除其后的一个 0，即可还原为 <code>0110111110111110</code>，从而保证数据不会与标志 <code>01111110</code> 混淆。</p>'
          },
          {
            no: '例 3.4', meta: '基础 · 字节填充',
            q: '面向字节的协议以 <code>7E</code> 为帧标志、<code>7D</code> 为转义字符，转义规则是把被转义字节与 <code>20H</code> 异或。要发送的数据字节序列为 <code>7E 7D 3A</code>，写出填充后的输出，并说明接收方的还原过程。',
            sol: '<p>7E 需改写为 7D 5E（7E ⊕ 20 = 5E），7D 需改写为 7D 5D（7D ⊕ 20 = 5D），3A 不变。</p><p>输出为 <b>7D 5E 7D 5D 3A</b>。接收方一见 7D 就删除它并把后一字节与 20H 异或，还原得 <code>7E 7D 3A</code>，数据中的 7E 不会被误认为帧标志，实现透明传输。</p>'
          }
        ],
        pitfalls: [
          '字符计数法一旦计数字段出错，后续帧定界全部错乱。',
          '零比特填充是在发送方插入 0，接收方遇到连续 5 个 1 后的 0 就删除。',
          '填充只作用于帧的数据部分，帧标志本身不参与填充。'
        ]
      },
      {
        id: 'ch3-s2', num: '3.2', title: '差错控制',
        lead: '奇偶校验只能检错，CRC 能高效检错，海明码可纠错。',
        blocks: [
          { t: 'h3', idx: '①', text: '检错编码' },
          { t: 'table', head: ['编码', '原理', '能力'], rows: [
            ['奇偶校验', '增加 1 位使 1 的个数为奇/偶', '只能检出奇数个位错'],
            ['循环冗余 CRC', '用生成多项式除数据，余数作 FCS', '检错能力强，广泛用于链路层']
          ]},
          { t: 'viz', build: 'crc', title: 'CRC 计算与校验', sub: '输入数据与生成多项式，观察模 2 除法' },
          { t: 'fml', rows: [
            { line: '发送：数据后补 r 个 0，除以 G(x)，余数即 FCS', note: 'r = G(x) 的阶' },
            { line: '接收：用同一 G(x) 除接收帧，余数为 0 则认为无错', note: '' }
          ]},
          { t: 'algo', name: '循环冗余校验 CRC', role: '检错编码', cost: '生成多项式阶 r · FCS 为 r 位余数',
            goal: '用生成多项式对数据做模 2 除法，取余数作 FCS 附在数据后，供接收方检错。',
            inputs: '待发送的数据比特串与双方约定的生成多项式 G(x)（阶为 r，最高位、最低位均为 1）。',
            steps: [
              '由生成多项式确定阶数 r：G(x) 的位数减 1 即为 r。',
              '在数据后补 <b>r 个 0</b>，作为模 2 除法的被除数。',
              '用 G(x) 对补 0 后的数据做<b>模 2 除法</b>（按位异或，不进位不借位）。',
              '取余数（不足 r 位时高位补 0）作为 r 位帧检验序列 <b>FCS</b>。',
              '把 FCS 拼接在原始数据之后，得到实际发送的码字。',
              '接收方用同一 G(x) 去除收到的码字：余数为 <b>0</b> 判为无错，否则丢弃并要求重传。'
            ],
            keys: [
              '模 2 除法只用异或，商不参与校验，真正有用的是余数（FCS）。',
              '发送的是“原数据 + FCS”，接收方除完后余数应为 0；余数为 0 也不能 100% 断言无错。',
              'CRC 只能检错不能纠错，生成多项式最高位与最低位必须为 1。'
            ]
          },
          { t: 'card', kind: 'tip', tag: '海明码', title: '纠错编码', html:
            '<p class="tight">海明码通过多个校验位定位并纠正一位错误。若信息位 n、校验位 k，需满足 2ᵏ ≥ n + k + 1；检错是“知道错了”，纠错是“知道错在哪并改正”。</p>'
          },
          { t: 'viz', build: 'hammingCode', title: '海明码编码与纠错', sub: '输入数据并指定翻转位，翻转位随码长动态覆盖全部校验位与数据位（0~码长）' },
          { t: 'algo', name: '海明码编码与一位纠错', role: '纠错编码', cost: '校验位 k 满足 2ᵏ ≥ n + k + 1 · 纠正一位错',
            goal: '在信息位之间插入多位校验位，用伴随式定位并纠正一位错误。',
            inputs: 'n 位信息位；据此确定校验位数 k，使 2ᵏ ≥ n + k + 1。',
            steps: [
              '确定校验位数 k，把各位按 1、2、3… 编号，编号为 <b>2 的幂</b>的位置留给校验位。',
              '把 k 个校验位依次放在 1、2、4、8… 号位置，其余位置填入信息位。',
              '让每个校验位负责校验编号二进制中对应位为 1 的数据位，按<b>偶校验</b>确定其取值。',
              '把校验位与信息位组成完整的海明码发送出去。',
              '接收方按同样的分组重新计算各组校验结果，得到<b>伴随式</b>。',
              '伴随式全 0 判为无错；否则伴随式之值就是出错位的编号，取反该位完成纠错。'
            ],
            keys: [
              '校验位放在 1、2、4、8… 等 2 的幂位置；2ᵏ ≥ n + k + 1 是校验位数的下界。',
              '伴随式的值直接给出出错位编号；海明码能纠正 1 位错，两位错只能发现。',
              '再加一个全校验位可区分一位错与两位错，避免把两位错误“纠正”成第三位。'
            ]
          }
        ],
        examples: [
          {
            no: '例 3.1', meta: 'CRC',
            q: '数据 101001，生成多项式 1001，求 FCS 与发送帧。',
            sol: '<p>补 3 个 0 得 101001000，用 1001 做模 2 除法，余数为 <b>001</b>。发送帧 = 数据 + FCS = <b>101001001</b>。</p>'
          }
        ],
        pitfalls: [
          'CRC 只能检错不能纠错（除非额外设计）。',
          '模 2 除法用异或，不进位不借位。',
          '生成多项式最高位与最低位必须为 1。'
        ]
      },
      {
        id: 'ch3-s3', num: '3.3', title: '流量控制与可靠传输机制',
        lead: '滑动窗口把“发送—确认—重传”组织起来：停止等待、后退 N 帧、选择重传。',
        blocks: [
          { t: 'h3', idx: '①', text: '三种可靠传输协议' },
          { t: 'viz', build: 'slidingWindow', title: '滑动窗口时空图', sub: '停止等待 / GBN / SR 对比（发送窗口可调至 12，可指定出错帧）' },
          { t: 'table', head: ['协议', '发送窗口', '接收窗口', '出错处理'], rows: [
            ['停止等待', '1', '1', '超时重传该帧'],
            ['后退 N 帧 GBN', '≤ 2ⁿ−1', '1', '出错后重传该帧及其后续所有帧'],
            ['选择重传 SR', '≤ 2ⁿ⁻¹', '≤ 2ⁿ⁻¹', '只重传出错的帧']
          ]},
          { t: 'fml', rows: [
            { line: '信道利用率 U = (W × T_d) / (T_d + 2T_p)', note: 'T_d 发送时延，T_p 单程传播时延' },
            { line: '停止等待 U = T_d / (T_d + 2T_p)', note: 'W = 1' }
          ]},
          { t: 'algo', name: '停止等待协议', role: '滑动窗口可靠传输', cost: '发送窗口 = 1 · 接收窗口 = 1 · U = T_d/(T_d+2T_p)',
            goal: '每发送一个帧就停下等待确认，用超时重传与序号机制保证帧不丢、不重。',
            inputs: '序号用 1 位（0/1 交替）；信道可能丢帧或出错，需要计时器与确认机制。',
            steps: [
              '发送方发送一个帧并启动<b>超时计时器</b>，在收到确认前不再发送下一帧。',
              '接收方校验该帧：正确则交付上层并回送<b>确认帧 ACK</b>。',
              '发送方收到确认后取消计时器、翻转序号，再发送下一个帧。',
              '若收到序号重复的帧，接收方<b>丢弃并重发确认</b>，避免重复交付。',
              '若计时器超时仍未收到确认，发送方<b>重传原帧</b>并重启计时器。',
              '重复上述过程直到数据发完，发送窗口与接收窗口始终为 1。'
            ],
            keys: [
              '停止等待只需 1 位序号，因为同一时刻至多有一个未被确认的帧。',
              '信道利用率 U = T_d / (T_d + 2T_p)，传播时延越大利用率越低。',
              '确认丢失或迟到时靠超时重传与“重复帧丢弃”保证既不多收也不漏收。'
            ]
          },
          { t: 'algo', name: '后退 N 帧协议（GBN）', role: '滑动窗口可靠传输', cost: '发送窗口 ≤ 2ⁿ−1 · 接收窗口 = 1',
            goal: '用滑动窗口实现连续发送与出错重传：接收方只按序接收，发送方无须逐帧等待确认。',
            inputs: '帧序号用 n 位表示；发送窗口 W ≤ 2ⁿ − 1，接收窗口固定为 1；链路可能丢帧或帧出错。',
            steps: [
              '发送方连续发送窗口内的帧，并为<b>每个已发送未确认的帧保留副本</b>与计时器。',
              '接收方只接收序号正确（按序）的帧，交付上层后回送<b>累计确认 ACKₙ</b>，表示 n 号及之前的帧全部收到。',
              '若收到失序帧（例如期待 3 却收到 4、5），接收方<b>直接丢弃</b>，并重复发送最后一次按序帧的确认。',
              '发送方每收到一个确认，窗口前沿向前滑动，可以继续发送新的帧。',
              '当某帧超时仍未收到确认，发送方<b>回退到该帧</b>，重传该帧及其后已在窗口内的所有帧。'
            ],
            keys: [
              '接收方没有缓存能力：失序帧一律丢弃，因此一次出错往往要重传后续一大段帧。',
              '发送窗口必须满足 W ≤ 2ⁿ − 1；若 W = 2ⁿ，接收方将无法区分新帧与超时重传的旧帧。',
              'GBN 采用累计确认；误码率高、带宽时延积大的链路更适合选择重传 SR。'
            ]
          },
          { t: 'algo', name: '选择重传协议（SR）', role: '滑动窗口可靠传输', cost: 'W_s + W_r ≤ 2ⁿ · 通常各取 2ⁿ⁻¹ · 每帧独立计时器',
            goal: '只重传出错或丢失的帧：接收方缓存失序帧，发送方逐帧确认、按需重传。',
            inputs: '序号用 n 位；窗口满足 W_s + W_r ≤ 2ⁿ；每个已发送未确认的帧各配一个计时器。',
            steps: [
              '发送方连续发送窗口内的帧，并为每个未确认帧<b>单独设置计时器</b>。',
              '接收方对每个正确到达的帧<b>逐一确认</b>，不采用累计确认。',
              '接收方可以接收并<b>缓存失序帧</b>，只把按序的部分交付上层。',
              '发送方收到某帧的确认就取消该帧的计时器，窗口前沿滑过连续已确认的部分。',
              '当某帧超时，发送方<b>只重传该帧</b>，已正确接收的其他帧不受影响。',
              '接收方把重传帧与缓存帧拼成有序数据交付上层，并相应滑动接收窗口。'
            ],
            keys: [
              'SR 允许失序接收与缓存，逐帧确认、按需重传，比 GBN 重传量小但实现复杂。',
              '必须满足 W_s + W_r ≤ 2ⁿ（常取二者各 2ⁿ⁻¹），否则接收方无法区分新帧与重传帧。',
              '每个未确认帧都要独立计时，这是 SR 区别于 GBN 的主要开销。'
            ]
          },
          { t: 'viz', build: 'protocolEfficiency', title: '信道利用率曲线', sub: '切换协议、调节窗口与时延，观察利用率如何逼近 100%' },
          { t: 'card', kind: 'key', tag: '必记', title: '窗口大小的限制', html:
            '<p class="tight">序号用 n 位表示时：GBN 发送窗口 W ≤ 2ⁿ − 1（避免接收方无法区分新帧与重传帧）；SR 要求 W_s + W_r ≤ 2ⁿ，通常取 W_s = W_r = 2ⁿ⁻¹。</p>'
          }
        ],
        examples: [
          {
            no: '例 3.2', meta: '信道利用率',
            q: '发送窗口 4，发送时延 1 ms，单程传播时延 3 ms。求 GBN 的信道利用率。',
            sol: '<p>U = (4 × 1) / (1 + 2×3) = 4 / 7 ≈ <b>57.1%</b>。若改为停止等待，U = 1/7 ≈ 14.3%，可见窗口能显著提高利用率。</p>'
          }
        ],
        pitfalls: [
          'GBN 接收窗口为 1，失序帧直接丢弃；SR 接收窗口大于 1，可缓存失序帧。',
          '序号位宽限制了窗口最大值，这是常考的数值关系。'
        ]
      },
      {
        id: 'ch3-s4', num: '3.4', title: '介质访问控制',
        lead: '多个结点共享信道时，用信道划分或随机访问来协调使用。',
        blocks: [
          { t: 'h3', idx: '①', text: '信道划分与随机访问' },
          { t: 'table', head: ['类别', '方法', '特点'], rows: [
            ['信道划分', 'FDM 频分、TDM 时分、WDM 波分、CDM 码分', '预分配，无冲突，适合重负载'],
            ['随机访问', 'ALOHA、CSMA、CSMA/CD、CSMA/CA', '竞争使用，轻负载效率高'],
            ['轮询访问', '令牌传递', '无冲突，重负载效率高']
          ]},
          { t: 'viz', build: 'csmaCd', title: 'CSMA/CD 工作流程', sub: '先听后发、边发边听、冲突退避' },
          { t: 'algo', name: 'CSMA/CD 工作流程', role: '总线以太网的介质访问控制', cost: '争用期 2τ · 最短帧长 = 2τ × 数据率',
            goal: '在共享总线上先听后发、边发边听，冲突时立即停发并退避重发。',
            inputs: '站点有帧待发且信道为共享总线（半双工）；帧长不小于最短帧长，保证能检测到冲突。',
            steps: [
              '发送前先<b>监听信道</b>：若信道忙则持续等待，直到变为空闲。',
              '信道空闲后开始发送，并<b>边发送边监听</b>，比较发出与收到的信号是否一致。',
              '若检测到冲突，立即<b>停止发送</b>并发出强化干扰信号，通知各站冲突已经发生。',
              '执行截断二进制指数退避，随机等待一段时间后重新监听并尝试发送。',
              '若一直未检测到冲突，则继续发送直到整帧发送完毕。',
              '若重传 16 次仍不成功，则丢弃该帧并向上层报告错误。'
            ],
            keys: [
              '“先听后发”避免撞上正在传输的帧，“边发边听”才能在发送过程中发现冲突。',
              '帧长必须 ≥ 争用期 2τ × 数据率（以太网最短帧 64 B），否则发完才发现冲突就来不及了。',
              'CSMA/CD 用于半双工有线以太网；无线网络无法边发边听，改用 CSMA/CA。'
            ]
          },
          { t: 'fml', rows: [
            { line: '争用期（冲突窗口）= 2τ，τ 为单程传播时延', note: '' },
            { line: '最短帧长 = 2τ × 数据率', note: '确保发送完毕前能检测到冲突' },
            { line: '退避：k ∈ [0, 2^min(重传次数,10) − 1]，等待 k × 2τ', note: '二进制指数退避' }
          ]},
          { t: 'algo', name: '截断二进制指数退避', role: 'CSMA/CD 冲突后的重传', cost: 'k = min(重传次数, 10) · 等待 r × 2τ · 16 次后放弃',
            goal: '让冲突各方随机等待不同时间后重发，冲突越多退避范围越大。',
            inputs: '本次冲突是第 i 次重传；争用期 2τ 由数据率与网段长度决定。',
            steps: [
              '发生冲突后统计这是第几次重传，记为 <b>i</b>。',
              '取指数 k = min(i, 10)，确定随机数范围为 [0, 2ᵏ − 1]。',
              '从该范围中随机取整数 <b>r</b>，得到退避时间 r × 2τ。',
              '等待该退避时间后重新监听信道：空闲则发送，忙则继续等待。',
              '若再次发生冲突，重传次数加 1，重复上述过程。',
              '当重传 16 次仍不成功时，丢弃该帧并报告错误，不再无限重试。'
            ],
            keys: [
              '指数上限取 10（即“截断”），退避范围最大为 [0, 2¹⁰ − 1]，避免范围无限制增大。',
              '退避时间以争用期 2τ 为单位，冲突次数越多随机范围越大，再次冲突概率越小。',
              '重传 16 次仍失败则丢弃，防止网络重载时无效重试加剧拥塞。'
            ]
          },
          { t: 'viz', build: 'binaryBackoff', title: '二进制指数退避模拟', sub: '逐步观察两站退避、冲突与重发；退避指数 k = min(重传次数,10)，最多重传 16 次' },
          { t: 'card', kind: 'exam', tag: '真题视角', title: '最短帧长', html:
            '<p class="tight">以太网最短帧 64 B。计算：若往返时延 2τ = 51.2 µs（100 Mbps 以太网），则最短帧 = 51.2 µs × 100 Mbps = 512 bit = 64 B。数据不足需填充。</p>'
          }
        ],
        examples: [
          {
            no: '例 3.5', meta: '计算 · 最短帧长',
            q: '某以太网数据率 10 Mb/s，最短帧长 64 B，信号传播速率 2×10⁸ m/s。求争用期 2τ 的值；若两站位于网段两端，最大距离是多少？',
            sol: '<p>最短帧的发送时间即为争用期：2τ = 512 bit ÷ 10×10⁶ bit/s = <b>51.2 µs</b>（即 τ = 25.6 µs）。</p><p>往返距离 = 2τ × v = 51.2×10⁻⁶ s × 2×10⁸ m/s = 10240 m，故单程最大距离约 <b>5120 m</b>。这说明最短帧长必须保证发送完毕之前能检测到冲突。</p>'
          },
          {
            no: '例 3.6', meta: '计算 · 二进制指数退避',
            q: '某站参与 CSMA/CD 竞争，争用期 2τ = 51.2 µs。求第 1 次冲突和第 3 次冲突后，退避时间 k × 2τ 的取值范围。',
            sol: '<p>第 i 次冲突后 k ∈ [0, 2^min(i,10) − 1]，等待时间为 k × 2τ。</p><p>第 1 次冲突：k ∈ {0, 1}，等待 <b>0 或 51.2 µs</b>；第 3 次冲突：k ∈ [0, 7]，等待 <b>0 ~ 7 × 51.2 = 0 ~ 358.4 µs</b>。</p><p>冲突次数越多，可选退避范围越大，从而降低再次冲突的概率。</p>'
          }
        ],
        pitfalls: [
          'CSMA/CD 用于有线以太网（半双工），CSMA/CA 用于无线（无法边发边听检测冲突）。',
          '冲突检测必须在发送完毕之前完成，否则无法判断冲突。',
          '各站退避时间随机，冲突次数越多退避范围越大。',
          '最短帧长 = 争用期 2τ × 数据率，由数据率与网段长度共同决定，与帧的内容无关。'
        ]
      },
      {
        id: 'ch3-s5', num: '3.5', title: '局域网与数据链路层设备',
        lead: '以太网用 MAC 地址标识站；交换机通过自学习建立转发表，隔离冲突域。',
        blocks: [
          { t: 'h3', idx: '①', text: '以太网与 MAC 地址' },
          { t: 'list', items: [
            '以太网在共享信道（半双工）下使用 CSMA/CD，早期采用曼彻斯特编码；',
            'MAC 地址 48 位，前 24 位为厂商编号，后 24 位为序列号；',
            '以太网 MAC 帧：目的地址、源地址、类型、数据、FCS，最短 64 B，最长 1518 B。'
          ]},
          { t: 'viz', build: 'switchLearn', title: '交换机自学习与转发', sub: '逐步观察 MAC 地址表的建立' },
          { t: 'algo', name: '交换机自学习算法', role: 'MAC 地址表建立与转发决策', cost: '表项含端口与老化时间 · 未知目的泛洪',
            goal: '根据帧的源 MAC 与入端口建立 MAC 地址表，据此单播转发或泛洪。',
            inputs: '交换机各端口不断收到帧；MAC 地址表初始为空，表项含 MAC、端口与老化计时。',
            steps: [
              '收到帧后读取其<b>源 MAC 地址</b>与入端口，把“MAC → 端口”写入地址表或刷新其老化时间。',
              '读取帧的<b>目的 MAC 地址</b>，在地址表中查找对应的出端口。',
              '若命中且出端口与入端口不同，只从该端口<b>单播</b>转发。',
              '若命中但出端口就是入端口，说明目的站与本帧同一网段，<b>丢弃</b>该帧。',
              '若未命中，则向除入端口外的所有端口<b>泛洪</b>该帧。',
              '长时间不用的表项到期后被老化删除，以适应主机移动与端口变化。'
            ],
            keys: [
              '口诀“学习源 MAC、查目的 MAC”：学习与转发决策在同一次收帧中一起完成。',
              '命中单播、未命中泛洪；泛洪不等于广播，未知目的的单播帧也会被泛洪。',
              'MAC 地址表项有老化时间；交换机隔离冲突域但不隔离广播域。'
            ]
          },
          { t: 'table', head: ['设备', '工作层次', '冲突域', '广播域'], rows: [
            ['中继器/集线器', '物理层', '同一冲突域', '同一广播域'],
            ['网桥/交换机', '数据链路层', '每端口一个冲突域', '默认同一广播域（VLAN 可划分）'],
            ['路由器', '网络层', '隔离', '隔离广播域']
          ]},
          { t: 'card', kind: 'key', tag: '必记', title: 'VLAN', html:
            '<p class="tight">VLAN 把一个物理局域网在逻辑上划分为多个广播域，同一 VLAN 内的主机才能直接通信，跨 VLAN 需经路由器或三层交换机，从而提高安全性与减少广播风暴。</p>'
          }
        ],
        examples: [
          {
            no: '例 3.7', meta: '应用 · 交换机自学习',
            q: '交换机 MAC 地址表初始为空。主机 A 从端口 1 发送帧给主机 B（B 不在表中，位于端口 3），B 收到后从端口 3 回帧给 A。说明交换机的处理过程及地址表的变化。',
            sol: '<p>① A 的帧到达端口 1：交换机学习“A → 端口 1”，查目的 B 未命中，于是向除端口 1 外的所有端口<b>泛洪</b>；</p><p>② B 的应答从端口 3 到达：学习“B → 端口 3”，查目的 A 已命中，只从<b>端口 1 单播</b>转发。</p><p>此后 A、B 之间的帧不再泛洪，直接定向转发；表项长时间不用会被老化删除。</p>'
          },
          {
            no: '例 3.8', meta: '计算 · 冲突域与广播域',
            q: '一台 24 端口交换机连接 23 台主机，第 24 个端口与一台 8 端口集线器相连，集线器其余 7 个端口各接一台主机。求冲突域与广播域的数量。',
            sol: '<p>交换机每个在用端口都是一个冲突域；集线器不隔离冲突域，其整个端口组只算一个冲突域。</p><p>冲突域 = 23 + 1 = <b>24 个</b>（23 个直连主机端口 + 集线器所在的一段）；交换机与集线器都不隔离广播域，故广播域 = <b>1 个</b>。</p>'
          }
        ],
        pitfalls: [
          '交换机隔离冲突域但不隔离广播域；路由器二者都隔离。',
          '交换机的转发表（MAC 地址表）通过“学习源 MAC”建立，表项有老化时间。',
          '自学习：收到帧记录源 MAC 与入端口；查目的 MAC，命中则定向转发，未命中则泛洪。',
          '泛洪不等于广播：未知目的 MAC 的帧会从除入端口外的所有端口转发，而广播帧是目的 MAC 为全 F 的帧。'
        ]
      }
    ]
  };
})(window);
