/* ============================================================
   ch5.js — 第5章 传输层
   ============================================================ */
(function (global) {
  'use strict';
  global.CH5 = {
    id: 'ch5', no: '5', title: '传输层',
    subtitle: '传输层提供进程到进程的逻辑通信：UDP 简单高效，TCP 可靠可控。',
    tags: ['端口', '套接字', 'UDP', 'TCP', '三次握手', '四次挥手', '滑动窗口', '拥塞控制', '慢开始'],
    sections: [
      {
        id: 'ch5-s1', num: '5.1', title: '传输层提供的服务',
        lead: '传输层用端口标识进程，实现复用与分用，提供面向连接或无连接的服务。',
        blocks: [
          { t: 'h3', idx: '①', text: '传输层的功能' },
          { t: 'list', items: [
            '<b>进程到进程</b>的逻辑通信（网络层是主机到主机）；',
            '<b>复用与分用</b>：多个应用进程复用同一传输层协议，接收时按端口分用；',
            '<b>差错检测</b>：对报文段进行校验；',
            '提供<b>面向连接（TCP）</b>或<b>无连接（UDP）</b>的服务。'
          ]},
          { t: 'h3', idx: '②', text: '端口与套接字' },
          { t: 'table', head: ['端口范围', '名称', '说明'], rows: [
            ['0 ~ 1023', '熟知端口', '如 HTTP 80、HTTPS 443、DNS 53、SMTP 25、POP3 110'],
            ['1024 ~ 49151', '登记端口', '分配给具体应用'],
            ['49152 ~ 65535', '临时端口', '客户端运行时动态分配']
          ]},
          { t: 'card', kind: 'def', tag: '定义', title: '套接字', html:
            '<p class="tight">套接字 = IP 地址 + 端口号，唯一标识网络中的一个进程通信端点。一条 TCP 连接由四元组（源 IP、源端口、目的 IP、目的端口）唯一确定。</p>'
          },
          { t: 'viz', build: 'portSocket', title: '复用与分用', sub: '播放应用进程的报文段经同一传输层复用发出、按端口分用交付的过程' },
          { t: 'fml', rows: [
            { line: 'TCP 端口号 16 位，范围 0 ~ 65535', note: '' },
            { line: 'UDP 面向报文：不合并也不拆分应用报文', note: '' },
            { line: 'TCP 面向字节流：把应用数据看成无结构的字节流，按需分段', note: '' }
          ]}
        ],
        examples: [
          {
            no: '例 5.3', meta: '基础 · 套接字与四元组',
            q: '服务器 10.0.0.1 在 TCP 80 端口提供 Web 服务。同一主机 10.0.0.5 上的两个浏览器进程分别用临时端口 50000、50001 访问该服务器。服务器上会建立几条 TCP 连接？写出它们的四元组（套接字对）。',
            sol: '<p>建立 <b>2 条</b>连接。连接 1：<code>(10.0.0.5:50000 ↔ 10.0.0.1:80)</code>；连接 2：<code>(10.0.0.5:50001 ↔ 10.0.0.1:80)</code>。</p><p>两条连接的服务器端口都是 80，但四元组不同；传输层正是靠“源 IP、源端口、目的 IP、目的端口”唯一区分每条连接，实现分用。</p>'
          },
          {
            no: '例 5.4', meta: '辨析 · 面向字节流与面向报文',
            q: '应用进程一次交给传输层 3000 B 数据，MSS = 1000 B。分别说明用 TCP 和 UDP 传输时数据如何被拆分与交付，接收方能否感知报文边界。',
            sol: '<p>TCP 面向字节流：发送方把 3000 B 看作无结构的字节流，按 MSS 划分为 3 个各 1000 B 的报文段；接收方进程可能分多次读取，读取次数与报文段数没有对应关系，<b>不保留报文边界</b>。</p><p>UDP 面向报文：把 3000 B 作为一个 UDP 数据报整体封装（超过链路 MTU 时由 IP 层分片，到达目的主机后再由 IP 层重组），接收方一次交付给应用进程 3000 B，<b>保留报文边界</b>。</p>'
          }
        ],
        pitfalls: [
          '端口号标识进程，不是主机；同一端口在不同 IP 上可同时使用。',
          'TCP 面向字节流，不保留应用层报文边界；UDP 保留报文边界。',
          '复用是发送时多个进程共用一个协议，分用是接收时按端口交给对应进程。',
          '连接由四元组唯一确定，服务器可用同一个端口同时承载多条连接。'
        ]
      },
      {
        id: 'ch5-s2', num: '5.2', title: 'UDP 与 TCP 报文段',
        lead: 'UDP 首部只有 8 字节，TCP 首部最小 20 字节，字段决定了可靠与流控能力。',
        blocks: [
          { t: 'viz', build: 'tcpUdpHeader', title: 'UDP / TCP 首部格式', sub: '切换协议查看字段' },
          { t: 'table', head: ['维度', 'UDP', 'TCP'], rows: [
            ['连接', '无连接', '面向连接'],
            ['可靠性', '尽最大努力，可能丢包乱序', '确认重传，可靠有序'],
            ['首部', '8 字节固定', '20~60 字节'],
            ['传输方式', '面向报文', '面向字节流'],
            ['效率', '高、开销小、时延低', '相对低'],
            ['适用', '实时音视频、DNS、广播', '文件、网页、邮件等可靠场景']
          ]},
          { t: 'card', kind: 'key', tag: '必记', title: 'TCP 首部关键字段', html:
            '<ul class="none"><li><b>序号 seq</b>：本报文段数据第一个字节的序号；</li><li><b>确认号 ack</b>：期望收到的下一个字节序号，表示之前都已正确收到；</li><li><b>窗口</b>：接收方允许发送方发送的字节数，用于流量控制；</li><li><b>标志位</b>：SYN 建立连接、FIN 释放连接、ACK 确认、RST 复位、PSH 推送、URG 紧急。</li></ul>'
          },
          { t: 'card', kind: 'tip', tag: 'UDP 校验', html:
            '<p class="tight">UDP 校验和计算时需加上 12 字节的伪首部（源/目的 IP、协议号、UDP 长度），使校验也能覆盖到 IP 地址，防止误交付。</p>'
          }
        ],
        examples: [
          {
            no: '例 5.5', meta: '计算 · UDP 首部',
            q: '某 UDP 数据报首部中“长度”字段的值为 50（十进制）。求 UDP 数据部分的字节数；若 IPv4 下计算校验和需加 12 B 伪首部，校验和覆盖的总字节数是多少？',
            sol: '<p>UDP 长度字段 = UDP 首部 + 数据 = 50 B，首部固定 8 B，故数据部分 = 50 − 8 = <b>42 B</b>。</p><p>校验和覆盖范围 = 12 B 伪首部 + 50 B UDP 数据报 = <b>62 B</b>（伪首部只参与计算，不实际传输）。</p>'
          },
          {
            no: '例 5.6', meta: '计算 · TCP 序号与确认号',
            q: '主机甲向乙发送报文段 1：seq = 200，携带 300 B 数据；乙正确接收后回确认。随后甲发送报文段 2：seq = 500，携带 200 B 数据。分别写出乙两次确认报文段中的确认号。',
            sol: '<p>报文段 1 占用序号 200 ~ 499，乙期望收到的下一个字节序号为 500，故 ack = <b>500</b>。</p><p>报文段 2 占用序号 500 ~ 699，乙收到后 ack = <b>700</b>。序号以字节为单位，ack 表示“该序号之前的字节已全部正确收到”，属于累计确认。</p>'
          }
        ],
        pitfalls: [
          'ack 是“期望收到的下一个序号”，即累计确认。',
          'TCP 的序号以字节为单位，不是报文段编号。',
          'UDP 校验和是可选的（IPv4 下），TCP 校验和是必需的。',
          'TCP 序号以字节为单位（SYN、FIN 各占用一个序号）；UDP 的“长度”字段包含 8 B 首部。'
        ]
      },
      {
        id: 'ch5-s3', num: '5.3', title: 'TCP 连接管理',
        lead: '三次握手建立连接（防止失效请求、同步序号），四次挥手释放连接（半关闭）。',
        blocks: [
          { t: 'viz', build: 'tcpHandshake', title: '三次握手与四次挥手', sub: '单步查看报文与状态变化' },
          { t: 'card', kind: 'key', tag: '必记', title: '为什么是三次握手', html:
            '<p class="tight">三次握手可防止已失效的连接请求突然到达服务器造成错误连接，同时双方交换初始序号并确认对方收发能力。第四次挥手后主动方进入 <b>TIME_WAIT</b>，等待 2MSL 以确保最后的 ACK 到达并让旧报文消散。</p>'
          },
          { t: 'table', head: ['阶段', '报文', '进入状态'], rows: [
            ['建立 1', 'SYN=1, seq=x', '客户端 SYN_SENT'],
            ['建立 2', 'SYN=1, ACK=1, seq=y, ack=x+1', '服务器 SYN_RCVD'],
            ['建立 3', 'ACK=1, seq=x+1, ack=y+1', '双方 ESTABLISHED'],
            ['释放', 'FIN / ACK / FIN / ACK', 'TIME_WAIT → CLOSED']
          ]},
          { t: 'algo', name: 'TCP 三次握手', role: '连接建立', cost: 'SYN 占 1 个序号 · 初始序号 ISN 随机 · 前两次不带数据',
            goal: '双方交换初始序号并确认彼此的收发能力，同步建立一条 TCP 连接。',
            inputs: '客户端随机选取初始序号 x，服务器随机选取 y；服务器处于 LISTEN 状态。',
            steps: [
              '客户端发送 <b>SYN=1, seq=x</b>，请求建立连接，随后进入 SYN_SENT 状态。',
              '服务器回送 <b>SYN=1, ACK=1, seq=y, ack=x+1</b>，确认收到并请客户端确认，进入 SYN_RCVD。',
              '客户端再发 <b>ACK=1, seq=x+1, ack=y+1</b>，进入 ESTABLISHED 状态。',
              '服务器收到第三个报文段后也进入 <b>ESTABLISHED</b>，连接建立完成，双方可以双向传输数据。',
              '注意 SYN 消耗一个序号，所以确认号等于对方初始序号加 1，数据序号从其后继续排。',
              '若旧的失效连接请求到达服务器，会因缺少第三次握手而无法完成，从而避免建立错误连接。'
            ],
            keys: [
              '三次握手的同时双方各自发送一个 SYN 并各确认一次，协商初始序号并验证收发能力。',
              'SYN、FIN 各消耗一个序号；不带数据的 ACK 不消耗序号。',
              '第三次握手可以携带数据，前两次握手不能携带数据。'
            ]
          },
          { t: 'algo', name: 'TCP 四次挥手', role: '连接释放', cost: 'FIN 占 1 个序号 · 主动方 TIME_WAIT 等待 2MSL',
            goal: '双方各自关闭自己的发送方向（半关闭），确认数据交付完毕后释放连接。',
            inputs: '连接处于 ESTABLISHED；主动方与被动方都可能还有未发送完的数据。',
            steps: [
              '主动方发送 <b>FIN=1, seq=u</b>，表示数据已发完、不再发送新数据，进入 FIN_WAIT_1。',
              '被动方回送 <b>ACK=1, seq=v, ack=u+1</b>，进入 CLOSE_WAIT；主动方收到后进入 FIN_WAIT_2。',
              '此时连接处于<b>半关闭</b>状态：被动方仍可继续发送剩余数据，主动方仍须接收。',
              '被动方数据发完后发送 <b>FIN=1, ACK=1, seq=w, ack=u+1</b>，进入 LAST_ACK。',
              '主动方回送 <b>ACK=1, seq=u+1, ack=w+1</b>，进入 TIME_WAIT 并等待 2MSL。',
              '等待 2MSL 后主动方进入 CLOSED；被动方收到最后 ACK 后立即进入 CLOSED，连接彻底释放。',
              '若等待期间收到被动方重传的 FIN，主动方重发 ACK 并重新计时 2MSL。'
            ],
            keys: [
              '被动方收到 FIN 后可能还有数据要发，ACK 与 FIN 往往分开，因此释放通常需要四次。',
              'TIME_WAIT 等 2MSL：保证最后的 ACK 能到达，并让旧连接的报文在网络中消散。',
              'FIN 消耗一个序号，故确认号为收到 FIN 的 seq + 1；半关闭期间被动方仍可发数据。'
            ]
          },
          { t: 'card', kind: 'warn', tag: '注意', title: 'SYN 洪泛攻击', html:
            '<p class="tight">攻击者发送大量 SYN 但不完成第三次握手，使服务器半连接队列耗尽。防护可使用 SYN Cookie 等机制。</p>'
          }
        ],
        examples: [
          {
            no: '例 5.1', meta: '握手序号',
            q: '客户端初始序号为 1000，服务器为 3000。写出三次握手的 seq 与 ack。',
            sol: '<p>① C→S：SYN=1, seq=<b>1000</b>；② S→C：SYN=1, ACK=1, seq=<b>3000</b>, ack=<b>1001</b>；③ C→S：ACK=1, seq=<b>1001</b>, ack=<b>3001</b>。</p>'
          }
        ],
        pitfalls: [
          'SYN 和 FIN 各占一个序号。',
          'TIME_WAIT 持续 2MSL，是为了保证最后 ACK 可靠到达并回收旧连接报文。',
          '半关闭：一方发送 FIN 后仍可接收对方数据。'
        ]
      },
      {
        id: 'ch5-s4', num: '5.4', title: 'TCP 可靠传输与拥塞控制',
        lead: '超时重传与快速重传保证可靠；流量控制保护接收方，拥塞控制保护网络。',
        blocks: [
          { t: 'h3', idx: '①', text: '可靠传输与滑动窗口' },
          { t: 'viz', build: 'tcpWindow', title: '发送窗口与累计确认', sub: '调节窗口与确认位置' },
          { t: 'list', items: [
            '基于字节的滑动窗口，累积确认；',
            '超时重传：RTO 由往返时延 RTT 的加权平均与偏差动态计算（Karn 算法）；',
            '快速重传：收到 3 个重复 ACK 立即重传，不必等超时。'
          ]},
          { t: 'algo', name: '超时重传与 RTO 计算（Karn 算法）', role: 'TCP 可靠传输', cost: 'RTO = RTT_S + 4 × RTT_D · 重传时 RTO 加倍（指数退避）',
            goal: '用 RTT 的加权平均与偏差动态设置超时时间，并在重传时正确维护 RTT 样本。',
            inputs: '每个已发送未确认的报文段需要计时；需要测得报文段的往返时间样本 RTT。',
            steps: [
              '发送报文段时启动其<b>超时计时器</b>，若到期仍未收到对应确认则重传该报文段。',
              '用新样本更新加权平均往返时间：<b>RTT_S = (1−α) × 旧 RTT_S + α × 新样本</b>（α 常取 1/8）。',
              '更新偏差：RTT_D = (1−β) × 旧 RTT_D + β × |新样本 − 旧 RTT_S|（β 常取 1/4）。',
              '按 <b>RTO = RTT_S + 4 × RTT_D</b> 计算超时时间，计时器到期即重传。',
              '执行 <b>Karn 算法</b>：报文段只要重传过，其确认就有二义性，该 RTT 样本不用于更新估计。',
              '每次重传后把 RTO <b>加倍</b>（指数退避）；直到收到未重传报文段的确认，才恢复正常计算。'
            ],
            keys: [
              '重传报文段的 ACK 无法判断对应哪次发送，Karn 算法规定直接丢弃该 RTT 样本。',
              'RTO 与 RTT_S、RTT_D 有关：RTT 波动越大偏差越大，超时时间也应越长。',
              '超时重传时间每次加倍（指数退避），是一种应对持续拥塞的保护措施。'
            ]
          },
          { t: 'algo', name: '快速重传', role: 'TCP 可靠传输', cost: '收到 3 个重复 ACK 即重传 · 无需等 RTO 到期',
            goal: '根据重复 ACK 提前判定报文段丢失并立即重传，避免等待超时造成长时延。',
            inputs: '接收方对失序报文段重复回送确认号相同的 ACK（重复 ACK）。',
            steps: [
              '接收方收到失序报文段时暂存数据，并重复发送期望序号的 <b>重复 ACK</b>。',
              '发送方每收到一个重复 ACK，就知道有一个报文段可能丢失或乱序。',
              '当同一确认号重复出现<b> 3 次</b>时，发送方判定该报文段已丢失。',
              '立即<b>重传</b>该报文段，不必等待超时计时器到期。',
              '重传后按快恢复（或慢开始，视 TCP 版本）调整窗口，继续发送后续数据。'
            ],
            keys: [
              '要求 3 个重复 ACK 是为了容忍网络乱序，防止把乱序错误判为丢失。',
              '重复 ACK 的确认号相同，都是接收方当前期望的下一个字节序号。',
              '快速重传与快恢复通常配合使用（Reno）；只快重传不恢复会使窗口回到 1（Tahoe）。'
            ]
          },
          { t: 'h3', idx: '②', text: '流量控制与拥塞控制' },
          { t: 'card', kind: 'def', tag: '对比', title: '流量控制 vs 拥塞控制', html:
            '<ul class="none"><li><b>流量控制</b>：接收方通过窗口字段告知发送方可发送量，防止接收缓冲溢出（点对点）；</li><li><b>拥塞控制</b>：发送方根据网络拥塞程度调整发送速率，防止网络过载（全局）。</li></ul><p class="tight">发送窗口 = min(接收窗口, 拥塞窗口 cwnd)。</p>'
          },
          { t: 'viz', build: 'congestion', title: 'TCP 拥塞控制曲线', sub: '慢开始、拥塞避免、超时与快恢复' },
          { t: 'algo', name: '慢开始与拥塞避免', role: 'TCP 拥塞控制', cost: 'cwnd 初值 1 个 MSS · 门限 ssthresh · 先指数后线性',
            goal: '先用指数再用线性的方式增大拥塞窗口，探测网络容量而不过载。',
            inputs: '拥塞窗口 cwnd 初值为 1 个 MSS；慢开始门限 ssthresh 由题目或上次拥塞事件给定。',
            steps: [
              '初始令 cwnd = 1，此后每经过一个 RTT 就把 cwnd <b>翻倍</b>（1、2、4、8…）。',
              '当 cwnd 达到 <b>ssthresh</b> 时改用拥塞避免：每经过一个 RTT，cwnd 只加 1。',
              '实际发送窗口取 <b>min(接收窗口 rwnd, 拥塞窗口 cwnd)</b>，两者共同限制发送量。',
              '若发生超时：ssthresh = 当前 cwnd / 2，cwnd = 1，重新执行慢开始。',
              '若收到 3 个重复 ACK：ssthresh = 当前 cwnd / 2，进入快恢复，cwnd = ssthresh。',
              '随着窗口增长再次逼近新门限，重复上述过程，使发送速率在容量附近波动。'
            ],
            keys: [
              '慢开始并不慢：cwnd 按 1、2、4、8 指数增长，只是起点低。',
              '超时使 cwnd 回到 1 重新慢开始；快恢复把 cwnd 设为 ssthresh，二者不同。',
              '发送窗口 = min(rwnd, cwnd)：流量控制保护接收方，拥塞控制保护网络。'
            ]
          },
          { t: 'table', head: ['阶段', '规则'], rows: [
            ['慢开始', 'cwnd 从 1 开始，每经过一个 RTT 加倍（指数增长）'],
            ['拥塞避免', 'cwnd ≥ ssthresh 后，每 RTT 加 1（线性增长）'],
            ['超时', 'ssthresh = cwnd/2，cwnd = 1，重新慢开始'],
            ['快重传', '收到 3 个重复 ACK，立即重传丢失报文段'],
            ['快恢复', 'ssthresh = cwnd/2，cwnd = ssthresh，进入拥塞避免']
          ]},
          { t: 'algo', name: '快恢复', role: 'TCP 拥塞控制（Reno）', cost: 'ssthresh = cwnd/2 · cwnd = ssthresh · 此后线性增长',
            goal: '收到 3 个重复 ACK 时只把窗口减半并进入拥塞避免，避免像超时那样降到 1。',
            inputs: '已触发快速重传（收到 3 个重复 ACK），当前 cwnd 与 ssthresh 已知。',
            steps: [
              '收到第 3 个重复 ACK 后，令 <b>ssthresh = cwnd / 2</b>，门限减半。',
              '令 <b>cwnd = ssthresh</b>（部分实现取 ssthresh + 3，以补偿已离开网络的报文段）。',
              '重传丢失的报文段，之后每收到一个重复 ACK 就把 cwnd 加 1，继续发送新数据。',
              '当收到确认新数据的 ACK 时，把 cwnd 收缩为 <b>ssthresh</b>。',
              '此后转入拥塞避免阶段，cwnd 每 RTT 线性加 1，直到再次发生拥塞。'
            ],
            keys: [
              '快恢复适用于个别报文段丢失（而非严重拥塞），窗口只减半，避免吞吐量骤降。',
              '超时说明网络可能严重拥塞，才采用 cwnd = 1 重新慢开始。',
              'Reno 组合：3 个重复 ACK → 快重传 + 快恢复；超时 → 慢开始。'
            ]
          },
          { t: 'fml', rows: [
            { line: '发送窗口 = min(接收窗口 rwnd, 拥塞窗口 cwnd)', note: '' },
            { line: '超时后 cwnd = 1，ssthresh = cwnd/2', note: '' },
            { line: '快恢复后 cwnd = ssthresh（不回到 1）', note: '' }
          ]}
        ],
        examples: [
          {
            no: '例 5.2', meta: '拥塞窗口',
            q: '初始 ssthresh = 8。当 cwnd 增长到 12 时发生超时，之后 cwnd 与 ssthresh 如何变化？',
            sol: '<p>超时：ssthresh = 12/2 = <b>6</b>，cwnd = <b>1</b>，重新慢开始。cwnd 按 1、2、4 增长，到 6 时 cwnd ≥ ssthresh，转入拥塞避免线性增长。</p>'
          }
        ],
        pitfalls: [
          '超时使 cwnd 回到 1；快恢复使 cwnd = ssthresh，二者不同。',
          '0 窗口时发送方启动持续计时器，避免死锁。',
          '接收窗口是流量控制，拥塞窗口是拥塞控制，实际发送取较小者。'
        ]
      }
    ]
  };
})(window);
